(() => {
  "use strict";

  const tutorialId = (title) => `tutorial-${title
    .normalize("NFKD")
    .replace(/[\u0300-\u036f]/gu, "")
    .toLowerCase()
    .replace(/[^a-z0-9]+/gu, "-")
    .replace(/^-+|-+$/gu, "")}`;

  const section = (
    title,
    explanation,
    exampleCode,
    checklist,
    takeaway,
    commonPitfall,
    options = {},
  ) => ({
    id: tutorialId(title),
    title,
    explanation,
    exampleCode,
    checklist,
    takeaway,
    commonPitfall,
    sampleInput: Array.isArray(options.sampleInput)
      ? options.sampleInput.map(String)
      : [],
  });

  const runStep = (phase, action, evidence, why, whenStuck) => ({
    phase,
    action,
    evidence,
    why,
    whenStuck,
  });

  window.LEARNING_CONTENT = {
    meta: {
      totalExercises: 110,
      maxStars: 294,
      starRule: "A passed exercise awards the number of stars shown by its difficulty.",
    },

    ranks: [
      {
        level: 1,
        name: "PEP Explorer",
        minStars: 0,
        minExercises: 0,
        description: "You are learning to read Python as a precise set of instructions.",
        accent: "#8b95a7",
      },
      {
        level: 2,
        name: "Indent Apprentice",
        minStars: 8,
        minExercises: 3,
        description: "Your first programs run, and indentation is becoming a useful tool rather than a surprise.",
        accent: "#3ccf91",
      },
      {
        level: 3,
        name: "Loop Tamer",
        minStars: 27,
        minExercises: 10,
        description: "You can control repetition, trace state, and stop loops deliberately.",
        accent: "#35b8ff",
      },
      {
        level: 4,
        name: "Function Forger",
        minStars: 55,
        minExercises: 22,
        description: "You shape small contracts into reusable functions with dependable return values.",
        accent: "#7c8cff",
      },
      {
        level: 5,
        name: "Collection Alchemist",
        minStars: 91,
        minExercises: 36,
        description: "Strings, lists, tuples, dictionaries, and sets transform cleanly in your hands.",
        accent: "#a77bff",
      },
      {
        level: 6,
        name: "Recursion Ranger",
        minStars: 132,
        minExercises: 52,
        description: "You can navigate nested structures and prove that recursive calls make progress.",
        accent: "#e06cff",
      },
      {
        level: 7,
        name: "Algorithm Architect",
        minStars: 181,
        minExercises: 72,
        description: "You choose representations, invariants, and complexity with intention.",
        accent: "#ff8a65",
      },
      {
        level: 8,
        name: "Pythonic Grandmaster",
        minStars: 294,
        minExercises: 110,
        description: "Every challenge is green; your solutions are clear, tested, and recognizably Pythonic.",
        accent: "#ffd166",
      },
    ],

    badges: [
      {
        id: "first_green",
        name: "First Green",
        icon: "✓",
        description: "Pass one exercise, including its hidden tests.",
        criteria: { metric: "passedExercises", operator: ">=", value: 1 },
      },
      {
        id: "ten_in_a_row",
        name: "Ten-Line Streak",
        icon: "10",
        description: "Build momentum by passing ten exercises overall.",
        criteria: { metric: "passedExercises", operator: ">=", value: 10 },
      },
      {
        id: "chapter_clear",
        name: "Chapter Clear",
        icon: "◆",
        description: "Pass every exercise in any one chapter.",
        criteria: { metric: "completedChapters", operator: ">=", value: 1 },
      },
      {
        id: "star_50",
        name: "Pocket Constellation",
        icon: "★",
        description: "Collect at least 50 difficulty stars.",
        criteria: { metric: "earnedStars", operator: ">=", value: 50 },
      },
      {
        id: "five_star_solver",
        name: "Five-Star Solver",
        icon: "Ⅴ",
        description: "Pass at least one difficulty-five exercise.",
        criteria: { metric: "highestPassedDifficulty", operator: ">=", value: 5 },
      },
      {
        id: "halfway_pythonic",
        name: "Halfway Pythonic",
        icon: "½",
        description: "Reach 50 percent overall exercise completion.",
        criteria: { metric: "progressPercent", operator: ">=", value: 50 },
      },
      {
        id: "collection_keeper",
        name: "Collection Keeper",
        icon: "{ }",
        description: "Complete Strings & Tuples, Lists, and Dictionaries & Sets.",
        criteria: {
          metric: "chaptersComplete",
          operator: "containsAll",
          value: ["py05", "py06", "py07"],
        },
      },
      {
        id: "recursion_unwound",
        name: "Recursion Unwound",
        icon: "↻",
        description: "Complete every recursion exercise without losing the base case.",
        criteria: {
          metric: "chapterComplete",
          operator: "==",
          value: true,
          chapterId: "py08",
        },
      },
      {
        id: "all_chapters",
        name: "Thirteen Keys",
        icon: "⌘",
        description: "Complete all thirteen chapters.",
        criteria: { metric: "completedChapters", operator: ">=", value: 13 },
      },
      {
        id: "full_constellation",
        name: "Full Constellation",
        icon: "✦",
        description: "Collect all 294 available difficulty stars.",
        criteria: { metric: "earnedStars", operator: ">=", value: 294 },
      },
    ],

    chapters: {
      py01: {
        title: "First Programs",
        subtitle: "Learn the workspace, then turn typed text into a small, explainable input → transform → output program.",
        objectives: [
          "Distinguish a program, source file, editor, Python interpreter, Run action, and terminal during one execution.",
          "Predict top-to-bottom execution and every exact character produced by print.",
          "Explain why input waits and returns str, then convert suitable whole counts and decimal measurements deliberately.",
          "Use variables as named checkpoints and classify beginner errors before changing code.",
          "Complete a predict → run → compare → explain → transfer learning cycle.",
        ],
        tutorial: [
          section(
            "Meet the program, file, editor, interpreter, and terminal",
            "A computer follows instructions, but it needs those instructions represented in a precise form. A program is an organized sequence of instructions. Python source code is the readable text used to express those instructions, and a source file is the saved container—commonly ending in .py—that holds the text. The editor changes that file. Pressing Run starts a new execution and asks the Python interpreter to read the current source. The terminal is the communication area that shows standard output and error reports; it is not the file itself. Python normally starts with the first statement and moves downward. Assignment stores a value, while print publishes a selected value and normally finishes the output line. After each source line, ask two separate questions: what changed in program state, and what exact characters became visible?",
            "status = \"Oven warming\"\ntemperature = 180\nprint(status)\nprint(temperature)",
            [
              "Point to the editor, source file, Run action, interpreter role, and terminal before writing code.",
              "Trace assignments and output in top-to-bottom order.",
              "Predict capitalization, spaces, punctuation, and line breaks before running.",
              "Change one source value, predict again, and explain why the next run differs.",
            ],
            "Mental model: the source file stores instructions, one run creates changing private state, and print exposes only selected values through the terminal.",
            "Saving, running, and printing are different actions. A correct stored value can remain invisible, while an extra prompt or debug line can make exact output incorrect.",
          ),
          section(
            "Watch input wait, return text, and resume",
            "When execution reaches input(), the program pauses at that line because it cannot continue until one complete input line is available. The supplied characters come from standard input; after Enter or an equivalent supplied line, input removes the line ending, returns the remaining characters as a str, and execution resumes at the next source line. Typing 12 therefore stores \"12\", not the integer 12. Convert at the boundary only when meaning requires arithmetic: int(raw_text) creates an exact whole number from valid integer text, while float(raw_text) creates a floating-point value from suitable decimal text. int(\"4.5\") and int(\"four\") raise ValueError because neither string represents an integer in the accepted form. The two-name form preserves the raw text as evidence and stores the converted value separately. A prompt inside input(\"...\") is output too, so omit it when an exact-output contract expects silent input.",
            "number = int(input())\naverage_weight = float(input())\nsummary = str(number) + \" crates\"\nprint(summary)\nprint(number * average_weight)",
            [
              "Predict the exact line where execution pauses and the exact line where it resumes.",
              "Record the raw value, its str type, the converted value, and the domain meaning separately.",
              "Choose int for whole counts, float for fractional measurements, and str for text that should remain characters.",
              "Check whether prompts are permitted by the output contract.",
            ],
            "Boundary rule: input always returns str; read raw text, convert only when the domain calls for another type, then let the core use stable values.",
            "Conversion is not guessing. int rejects decimal notation, float is approximate, and identifiers such as postal codes may need to remain strings so leading zeroes survive.",
            { sampleInput: ["3", "1.5"] },
          ),
          section(
            "Use variables as labelled memory, not magic boxes",
            "A variable is a name currently bound to a value. In an assignment such as pizzas = 3, Python evaluates the expression on the right and then binds the result to the name on the left; = here is an instruction, not a mathematical claim that both sides must stay equal forever. Assignment does not print. A later expression can read the name, and another assignment can bind that name to a new value. Choose names that describe meaning and, when useful, units. Keep raw text, converted data, intermediate calculations, and fixed constants under different names so a trace can show where the first unexpected value appears.",
            "slices_per_pizza = 8\npizzas = 3\nreserved_slices = 5\navailable_slices = pizzas * slices_per_pizza - reserved_slices\nprint(available_slices)",
            [
              "Read assignment from right to left: evaluate the value, then bind the name.",
              "Prefer names such as available_slices over unexplained single letters.",
              "Separate raw input, converted values, fixed rules, and calculated results.",
              "Write the value, type, and unit after every assignment for one sample run.",
            ],
            "Mental model: each well-named variable is a labelled checkpoint in one run's private state; print is a separate decision to reveal a checkpoint.",
            "Treating = as an equation or reusing one name for unrelated meanings hides when its value, type, or unit changed.",
          ),
          section(
            "Learn through predict, run, compare, explain, and transfer",
            "Before Run, trace the source from top to bottom and write the expected stored values, types, units, waiting point, and exact output. Run once without changing several things at once. Compare observation with prediction and locate the first source line where they differ. Explain that difference with a precise rule: perhaps input returned str, print added a line ending, a name was unavailable, or conversion rejected a value. Correct one cause, predict again, and finally transfer the same pattern to a different story and different values. Operators still follow precedence, so use named stages and parentheses to keep the transformation explainable.",
            "cups_of_flour = 2.5\ntablespoons_per_cup = 16\nextra_tablespoons = 3\ntotal_tablespoons = cups_of_flour * tablespoons_per_cup + extra_tablespoons\nprint(total_tablespoons)",
            [
              "Write one prediction with state and exact output before pressing Run.",
              "Compare at the first differing line rather than changing code by guesswork.",
              "Classify SyntaxError, NameError, TypeError, and ValueError before correcting the cause.",
              "Transfer the pattern to a fresh context and predict a normal plus zero-sized case.",
            ],
            "The learning loop is predict → run → compare → explain → transfer; an error report is evidence inside that loop, not a verdict on the learner.",
            "Changing many lines after a surprising result destroys evidence, while a program that runs can still be logically wrong through incompatible units or grouping.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "The workspace and three-room program",
            body: "First separate the saved source file from the execution that reads it. During that run, picture three connected rooms: input receives outside text, the workbench converts and transforms named values, and output publishes only the promised result. A bug becomes easier to locate when you can name the workspace part, room, and source line where reality first differs from your prediction.",
            steps: [
              { label: "Workspace", detail: "Identify the editor and saved .py source, then predict what a new interpreter run should reveal in the terminal." },
              { label: "Contract", detail: "List each incoming line in order and describe the exact visible result without writing Python yet." },
              { label: "Boundary", detail: "Read text and convert only quantities that genuinely need arithmetic; identifiers can remain strings." },
              { label: "State", detail: "Give every intermediate value a meaningful name and record its expected type and unit." },
              { label: "Transform", detail: "Apply one understandable relationship at a time and check it against a hand-worked example." },
              { label: "Publish", detail: "Print only the contract value, compare every visible character, explain the first difference, and transfer the pattern." },
            ],
          },
          guidedPractice: [
            {
              title: "Build a café collection note",
              prompt: "Predict when the program waits, then read a drink name and a whole-number quantity and create one clear summary line. Use your own wording so this remains separate from any graded exercise.",
              starterCode: "drink = input()\nquantity_text = input()\n\n# Convert the quantity and build a summary.\n# Print exactly one line.",
              questions: [
                "What types do the two names hold immediately after input()?",
                "Which value needs conversion, and at what boundary should it happen?",
                "How could an extra prompt alter an automated output comparison?",
                "After the run, what was the first difference from your prediction and which rule explains it?",
              ],
              reveal: "A sound plan has three visible stages: capture two strings, convert only the quantity, then construct and print one summary. Predict the wait points and final characters, run once, explain the first difference, then reuse the flow with an unrelated item.",
            },
            {
              title: "Trace a time conversion",
              prompt: "Turn a supplied number of whole minutes into a total number of seconds, but first annotate the wait point, raw str, converted type, unit, state, and exact output after every line.",
              starterCode: "minutes_text = input()\n# minutes = ...\nseconds_per_minute = 60\n# total_seconds = ...\n# print the result",
              questions: [
                "Why is multiplying the original text unsafe even though Python permits some string multiplication?",
                "Which name is fixed policy and which name varies per run?",
                "What should happen for zero minutes, and does the same data flow still work?",
                "How would you transfer the same boundary and naming pattern to a decimal measurement without copying this calculation?",
              ],
              reveal: "Convert the boundary value to an integer once, keep the raw text and fixed factor separate, and derive a newly named total. A zero case should follow the same stages; the transfer changes the domain and perhaps the numeric type, not the learning workflow.",
            },
          ],
          glossary: [
            { term: "program", definition: "An organized sequence of instructions intended for a computer to execute." },
            { term: "source file", definition: "A saved text container for program instructions; a Python source filename commonly ends with .py." },
            { term: "editor", definition: "The workspace used to view and change source text before another run." },
            { term: "interpreter", definition: "The program that reads Python source and carries out valid instructions during an execution." },
            { term: "terminal", definition: "A text communication interface that can show program output, accept input, and display error reports." },
            { term: "statement", definition: "One executable instruction, normally processed in source order." },
            { term: "string literal", definition: "Text written directly in source between matching quotation marks; the marks delimit the value but are not its visible characters." },
            { term: "function call", definition: "A request written with a callable name and parentheses, such as print(value) or input()." },
            { term: "standard input", definition: "The stream from which input() receives text, one requested line at a time." },
            { term: "standard output", definition: "The visible text stream produced by print and compared by many automated tests." },
            { term: "assignment", definition: "Evaluation of a right-hand expression followed by binding its value to a name." },
            { term: "variable", definition: "A name currently bound to a value during one execution of the program." },
            { term: "type conversion", definition: "Creating a value of another type, such as turning numeric text into an integer." },
            { term: "expression", definition: "Code that Python evaluates to produce a value." },
            { term: "traceback", definition: "Python's report of the calls and source location involved when an exception stops execution." },
          ],
          debugChecklist: [
            "Confirm the editor contains the intended source and that Run starts a new execution of the current text.",
            "Make one prediction before changing code, then locate the first source line where observation differs.",
            "Read the traceback from its final line upward and identify the first failing operation.",
            "Ask whether the failure is unreadable syntax, an unavailable name, an unsuitable type, or an unacceptable value.",
            "Write the value and type produced by every input() call before assuming it is numeric.",
            "Compare the required and observed output with spaces and line breaks made visible.",
            "Check units on both sides of every arithmetic operator.",
            "Temporarily inspect one intermediate value at a time, then remove diagnostics before testing.",
            "Re-run with zero or the smallest valid quantity to expose hidden assumptions.",
            "Transfer the corrected pattern to fresh names and values to prove the explanation was not memorized output.",
          ],
          checkpoint: {
            question: "A user types 12 when the program executes value = input(). What is stored in value?",
            options: ["The integer 12", "The string \"12\"", "A value whose type changes automatically when used", "Two individual integer digits"],
            answerIndex: 1,
            explanation: "input() always returns text. The program must request an explicit conversion when the domain calls for a number.",
          },
        },
        coachConversation: [
          { learner: "Is the terminal the same thing as my Python file?", coach: "No. The source file saves readable instructions, the editor changes them, and Run asks the interpreter to execute the current source. The terminal is a communication area for input, output, and errors produced during that execution." },
          { learner: "Why did nothing appear after I assigned a variable?", coach: "Assignment changes private program state; it does not publish anything. Use print(name) only when that value belongs in visible output, and remember that the terminal then receives a line ending too." },
          { learner: "I typed 12. Why does input() not behave like a number?", coach: "Because keyboard input crosses into Python as text. input() returns the string \"12\"; use number = int(input()) when the contract guarantees a whole number, or keep a raw_text variable first when you want easier debugging." },
          { learner: "Should I use int(), float(), or str()?", coach: "Use int() for exact whole counts, float() for decimal measurements, and str() when you deliberately need text. Convert once near the input boundary, then keep the stable numeric value for the calculation." },
          { learner: "Is type casting different from type conversion?", coach: "People often say casting, but explicit type conversion is the clearer Python term here: int(text), float(text), str(value), and bool(value) create new values of the requested type. They do not change the original object, and each conversion has its own acceptance rules." },
          { learner: "My calculation is right, but the test still fails.", coach: "Look at the public boundary. input(\"Number: \") prints a prompt, debug prints add lines, and tiny spacing differences change stdout. Also remember that int() raises ValueError if the supplied text is not a valid integer." },
          { learner: "What should I do when red error text appears?", coach: "Keep the evidence. Read the final exception line, find the referenced source line, and classify the first broken rule: SyntaxError, NameError, TypeError, or ValueError. Change one cause, predict the new result, then run again." },
        ],
        documentation: [
          { label: "Built-in input()", description: "How Python reads one line and returns it as text, including the visible prompt argument.", url: "https://docs.python.org/3/library/functions.html#input" },
          { label: "Built-in int()", description: "How Python converts valid whole-number text and which forms the integer constructor accepts.", url: "https://docs.python.org/3/library/functions.html#int" },
          { label: "Built-in float()", description: "How decimal text and compatible numeric values become floating-point approximations.", url: "https://docs.python.org/3/library/functions.html#float" },
          { label: "Input and output", description: "Printing, string formatting, and reading user-entered data.", url: "https://docs.python.org/3/tutorial/inputoutput.html" },
        ],
        runbook: [
          runStep("1. Orient and predict", "Identify the editor, source file, Run action, interpreter role, input source, and terminal; then predict the waiting point, stored values, and exact visible characters.", "A labelled workspace sketch and top-to-bottom prediction trace.", "Separating saved source from a running process prevents workspace confusion, while prediction creates evidence before trial and error begins.", "Point to each workspace part aloud, then number source lines and write state or output beside every line without pressing Run."),
          runStep("2. Run and observe once", "Supply input lines in their promised order, watch where execution waits and resumes, and record output or the final exception line without editing immediately.", "One observation transcript beside the untouched prediction.", "A single controlled run preserves the first difference that explains the behaviour; repeated random edits erase that evidence.", "Make spaces and line endings visible in the transcript and write str beside every value returned by input."),
          runStep("3. Compare and classify", "Circle the first differing source line, then classify it as workspace, syntax, name, type, value, unit, calculation, or exact-output evidence.", "A one-sentence explanation using the value, type, unit, or exception involved.", "The first difference is usually causal; later wrong values are often consequences and should not all be patched independently.", "For an error, read the final traceback line first; for output, compare capitalization, punctuation, spacing, prompts, and line count."),
          runStep("4. Correct one cause", "Change one source detail, predict the new state and output, then run an ordinary plus boundary case.", "Two prediction-versus-observation records with one controlled correction.", "Changing one cause connects the rule to the outcome and makes conversion, assignment, and print behaviour easier to remember.", "Keep raw text and converted values under separate names while testing int, float, zero, and one unsuitable value safely."),
          runStep("5. Explain, clean, and transfer", "Remove diagnostics, explain the complete input → conversion → transformation → output path, then rebuild the same pattern with fresh names, values, and domain meaning.", "Exact contract output plus a distinct transfer example and verbal explanation.", "Transfer demonstrates understanding rather than memorized characters, while boundary cleanup preserves the observable interface.", "Ask a partner to change the context from objects to measurements and justify whether the transferred conversion should use int, float, or remain str."),
        ],
      },

      py02: {
        title: "Simple Data",
        subtitle: "Represent numbers accurately and translate formulas into testable expressions.",
        objectives: [
          "Choose integer or floating-point operations deliberately.",
          "Translate formulas with correct grouping; distinguish nearest rounding, directed bounds, truncation, and display formatting.",
          "Use remainder and boolean values in numeric decisions.",
        ],
        tutorial: [
          section(
            "Choose a numeric meaning before an operator",
            "Integers represent exact whole counts. Floating-point values approximate measurements and ratios. The operator should match the question: / asks for a quotient that may contain a fraction, // asks how many complete groups fit, and % asks what remains. For negative operands, // rounds downward rather than toward zero, so confirm that behaviour if signs are allowed.",
            "pages = 53\ndays = 7\nexact_rate = pages / days\ncomplete_daily_sets = pages // days\nunassigned_pages = pages % days\nprint(exact_rate, complete_daily_sets, unassigned_pages)",
            [
              "State whether the result is a count, measurement, ratio, or remainder.",
              "Use / only when a fractional answer has meaning.",
              "Test // and % together to verify dividend == divisor * quotient + remainder.",
            ],
            "Mental model: operators are questions about the data, not interchangeable syntax choices.",
            "Converting a division result to int after the fact can hide the fact that the wrong kind of division was chosen.",
          ),
          section(
            "Preserve the structure of a formula",
            "Translate mathematical notation from the outside inward. Name quantities that carry meaning and subexpressions that are reused. Parentheses should reveal the intended grouping even when precedence would technically produce the same value. Once the code mirrors the written formula, a hand calculation can validate each stage independently.",
            "mass_kg = 72\nspeed_mps = 4.5\nspeed_squared = speed_mps ** 2\nkinetic_energy_joules = 0.5 * mass_kg * speed_squared\nprint(kinetic_energy_joules)",
            [
              "Write the formula with units before translating it.",
              "Compute meaningful repeated subexpressions only once.",
              "Check a simple case whose result can be derived mentally.",
            ],
            "Named stages make mathematical intent reviewable and localize mistakes.",
            "A compact one-line formula may be syntactically valid while silently using the wrong grouping or unit.",
          ),
          section(
            "Separate precision from presentation",
            "First decide whether the contract asks you to change a numeric value or only its display. round chooses a nearby numeric value and uses ties-to-even for exact halfway cases. math.floor and math.ceil choose lower and upper integer bounds, while int and math.trunc move toward zero. An f-string returns text with fixed visible precision. Keep the raw value until the last responsible moment, select the operation from the domain guarantee, and format only for display.",
            "import math\n\nmass_kg = 7 / 9\nrounded_mass_kg = round(mass_kg, 3)\nlower_bound = math.floor(mass_kg)\nupper_bound = math.ceil(mass_kg)\nlabel = f\"{mass_kg:.3f} kg\"\nprint(rounded_mass_kg, lower_bound, upper_bound)\nprint(label, type(label).__name__)",
            [
              "State whether the rule asks for nearest, a guaranteed lower bound, a guaranteed upper bound, or truncation toward zero.",
              "Import math before using math.floor or math.ceil; round and int are built-ins and need no import.",
              "Test positive, negative, exact-integer, and just-over-the-boundary values.",
              "Distinguish an int or float result from formatted text with fixed visible precision.",
            ],
            "Choose a rounding direction from the real-world guarantee you need; choose formatting separately for display.",
            "Treating floor, ceil, int, //, and round as synonyms breaks at negative values and at capacity boundaries; early rounding can also move the final result across a threshold.",
          ),
          section(
            "Use remainder to model repeating states",
            "Modulo maps an unbounded count into a fixed cycle. It can identify a position inside a repeating pattern, wrap an index, or test whether a complete group has no remainder. Write the possible remainder values first, then map each one to a meaning. Boundary cases occur immediately before, at, and after a full cycle.",
            "pattern = (\"red\", \"amber\", \"green\", \"blue\")\nstep_number = 14\npattern_index = step_number % len(pattern)\nactive_colour = pattern[pattern_index]\nprint(active_colour)",
            [
              "List all legal remainder values for the cycle.",
              "Explain what remainder zero means in this domain.",
              "Test one position before and after a wrap.",
            ],
            "Mental model: modulo folds a number line into a repeating ring of positions.",
            "Using a fixed cycle length in several places creates inconsistent behaviour when the pattern later changes; derive it with len.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "Value, unit, representation",
            body: "Every numeric name carries three ideas: the mathematical value, the unit that gives it meaning, and the Python representation used to store it. Reliable calculations keep these three aligned. Operators then become domain questions—fractional share, complete groups, remainder, power—instead of symbols chosen by habit.",
            steps: [
              { label: "Classify", detail: "Decide whether each quantity is an exact count, approximate measurement, ratio, or cyclic position." },
              { label: "Annotate", detail: "Attach a unit to every quantity and convert incompatible units before combining them." },
              { label: "Structure", detail: "Translate the formula into named stages whose grouping mirrors the written rule." },
              { label: "Compute", detail: "Retain useful internal precision and verify one simple value by hand." },
              { label: "Present", detail: "Apply nearest, floor, ceiling, or truncation only where the contract requires that rule, then format display text separately." },
            ],
          },
          interactiveLab: {
            kind: "rounding-boundaries",
            title: "Move across the number line",
            description: "Change one value and compare Python's four integer-boundary decisions. Watch the negative side carefully: floor follows the lower boundary, ceil follows the upper boundary, and int truncates toward zero.",
            min: -5,
            max: 5,
            defaultValue: -2.3,
            scenarios: [
              { label: "Negative fraction", value: -2.3, note: "Separates floor from truncation." },
              { label: "Near zero", value: -0.2, note: "Shows truncation reaching zero while floor moves left." },
              { label: "Positive fraction", value: 2.3, note: "Shows why floor and int often look identical for positive values." },
              { label: "Even tie", value: 2.5, note: "The even neighbour is 2." },
              { label: "Other tie", value: 3.5, note: "The even neighbour is now 4." },
              { label: "Exact integer", value: 3, note: "All four operations agree at an integer." },
            ],
            rules: [
              { intent: "Nearest integer", operation: "round(value)", reason: "Distance decides; an exact halfway tie selects the even candidate." },
              { intent: "Never exceed what fits", operation: "math.floor(value)", reason: "Choose the greatest integer that is still no larger than the value." },
              { intent: "Always cover the demand", operation: "math.ceil(value)", reason: "Choose the least integer that is still no smaller than the value." },
              { intent: "Discard the fraction", operation: "int(value) or math.trunc(value)", reason: "Move toward zero without claiming a lower, upper, or nearest guarantee." },
              { intent: "Count groups and leftovers", operation: "// and %", reason: "Express quotient-and-remainder meaning directly instead of dividing and converting afterward." },
              { intent: "Show fixed decimals", operation: "f\"{value:.2f}\"", reason: "Create display text without replacing the numeric value used by later calculations." },
            ],
            invariants: [
              "math.floor(value) <= value <= math.ceil(value)",
              "At an exact integer, floor, ceil, truncation, and the original value agree.",
              "math.ceil(value) == -math.floor(-value)",
            ],
          },
          guidedPractice: [
            {
              title: "Pack workshop kits",
              prompt: "Given a number of components and a fixed kit size, reason about complete kits and leftover components without solving any course exercise.",
              starterCode: "components = 47\nkit_size = 6\n# complete_kits = ...\n# leftovers = ...\n# verify the quotient/remainder identity",
              questions: [
                "Which operator asks for complete groups and which asks what remains?",
                "What identity should connect the original count, quotient, divisor, and remainder?",
                "What precondition must hold for kit_size?",
              ],
              reveal: "Model the result as a pair: complete groups and a remainder smaller than the positive group size. Validate the pair by reconstructing the original count rather than trusting two isolated outputs.",
            },
            {
              title: "Choose a batch boundary",
              prompt: "Use the same material-to-capacity ratio to reason about complete batches that fit, containers needed to hold everything, and a separately formatted utilization label.",
              starterCode: "import math\n\nmaterial_kg = 18.2\nbatch_capacity_kg = 4.0\nratio = material_kg / batch_capacity_kg\n# complete_batches = ...\n# containers_needed = ...\n# utilization_label = ...",
              questions: [
                "Predict both counts for material values 16.0, 15.9, and 16.1 before running code.",
                "Which invariant connects math.floor(ratio), ratio, and math.ceil(ratio)?",
                "Why is int(ratio) a weaker explanation than floor or //, even when this positive sample gives the same number?",
                "Why must the formatted label remain separate from later arithmetic?",
              ],
              reveal: "Boundary triplets reveal the contract. At an exact multiple, the lower and upper integer bounds meet. Just above it, the upper bound increases; just below it, the lower bound decreases. Choose the operation that states the guarantee, not merely one that matches the current sample, and keep display text outside later arithmetic.",
            },
          ],
          glossary: [
            { term: "integer", definition: "An exact whole-number value with no fractional component." },
            { term: "floating point", definition: "A finite binary approximation used for many real-valued measurements." },
            { term: "floor division", definition: "Division with // whose result is rounded down to a whole quotient." },
            { term: "floor", definition: "The greatest integer less than or equal to a value; for example, floor(-2.3) is -3." },
            { term: "ceiling", definition: "The least integer greater than or equal to a value; for example, ceil(-2.3) is -2." },
            { term: "truncation", definition: "Discarding a fractional part toward zero; for example, int(-2.3) and trunc(-2.3) are -2." },
            { term: "directed rounding", definition: "Choosing a boundary in a required direction, such as downward with floor or upward with ceiling, instead of choosing the nearest value." },
            { term: "modulo", definition: "The % operation that returns a remainder and can model repeating cycles." },
            { term: "precedence", definition: "Rules determining which operations bind first when parentheses do not state grouping." },
            { term: "format specification", definition: "Instructions inside formatted text that control visible width, alignment, or precision." },
          ],
          debugChecklist: [
            "Write the intended unit beside every numeric name and intermediate result.",
            "Check every divisor for a possible zero value before division.",
            "Add parentheses that mirror the written formula, even when precedence would agree.",
            "Verify // and % together by reconstructing the dividend.",
            "Compare the unrounded number, rounded number, and formatted string as three separate artifacts.",
            "Test values exactly on and immediately beside important numeric boundaries, including a negative fraction when signs are permitted.",
          ],
          checkpoint: {
            question: "A shuttle holds 12 people and all 25 people must travel. Which expression gives the minimum number of shuttles?",
            options: ["25 / 12", "25 // 12", "math.floor(25 / 12)", "math.ceil(25 / 12)"],
            answerIndex: 3,
            explanation: "The capacity must cover the entire demand, so the required whole count is the upper integer boundary. Floor division or math.floor would leave one person without a seat.",
          },
        },
        coachConversation: [
          { learner: "This input looks numeric. Can I calculate with it immediately?", coach: "Not yet: input() returned a str. Parse a whole count with count = int(input()) or a decimal measurement with distance = float(input()), then keep the chosen type consistent through the formula." },
          { learner: "Which division operator should I choose?", coach: "Ask the domain question aloud. Use / for a fractional ratio, // for complete groups, and % for the remainder. For a sanity check, quotient * divisor + remainder should rebuild the original whole count." },
          { learner: "Why did a correct-looking decimal fail?", coach: "Separate calculation from presentation. Keep useful precision internally; apply round() or an f-string format only where the contract requests it, and test values just beside the rounding boundary." },
          { learner: "How do I choose between round, floor, ceil, int, and //?", coach: "Start with the guarantee. round chooses the nearest value and uses ties-to-even for exact halfway cases. math.floor is the lower integer boundary, math.ceil is the upper one, and int on a float truncates toward zero. The difference is visible at -2.3: -3, -2, and -2 respectively. Use // when the domain is quotient or complete-group division rather than a generic conversion after /." },
          { learner: "Why did math.ceil produce one extra unit when the value printed as a whole number?", coach: "Inspect repr(value) and trace how the value was calculated. A displayed 3.0 can be slightly above 3 internally. Do not repair that with an arbitrary epsilon: prefer integer arithmetic for exact counts, or define a domain tolerance and use math.isclose only when near-enough values are genuinely equivalent." },
          { learner: "What is the difference between a built-in, module, package, and library?", coach: "Built-ins such as round need no import. A module is one importable namespace, a package organizes modules, and library is the broad informal name for reusable code. Python's standard library ships with Python; third-party packages must be installed in the runtime before import can find them." },
        ],
        documentation: [
          { label: "Numeric types", description: "Integer and floating-point behaviour, operators, and mixed numeric calculations.", url: "https://docs.python.org/3/library/stdtypes.html#numeric-types-int-float-complex" },
          { label: "Arithmetic expressions", description: "Official precedence and semantics for arithmetic, floor division, remainder, and powers.", url: "https://docs.python.org/3/reference/expressions.html#binary-arithmetic-operations" },
          { label: "Built-in round()", description: "How rounding precision and tie behaviour work.", url: "https://docs.python.org/3/library/functions.html#round" },
          { label: "math.floor(), math.ceil(), and math.trunc()", description: "Official definitions for lower bounds, upper bounds, and truncation toward zero.", url: "https://docs.python.org/3/library/math.html#number-theoretic-functions" },
          { label: "Floating-point arithmetic", description: "Why many decimal values are approximations and why displayed equality can hide a nearby stored value.", url: "https://docs.python.org/3/tutorial/floatingpoint.html" },
          { label: "Modules and imports", description: "How module namespaces, import forms, search paths, and packages work.", url: "https://docs.python.org/3/tutorial/modules.html" },
        ],
        runbook: [
          runStep("1. Classify quantities", "Label each value as a count, measurement, ratio, remainder, or formatted string.", "A justified Python type and unit for every value.", "The representation and operator should follow the domain meaning; choosing them together prevents accidental truncation and meaningless arithmetic.", "Make a two-column list headed exact and approximate, then place every input and result in one column with its unit."),
          runStep("2. Translate the rule", "Preserve grouping with named stages instead of compressing the formula immediately.", "A hand trace from inputs through intermediate values.", "Visible stages let you compare Python with the mathematical relationship one subexpression at a time.", "Replace the formula with placeholder names for its innermost groups, calculate those names first, and rebuild outward."),
          runStep("3. Map the domain", "Identify zero divisors, invalid roots, sign assumptions, and cycle boundaries before calculation.", "A short list of legal and illegal cases.", "Numeric syntax accepts operations whose inputs may be meaningless or undefined in the problem domain.", "Write the precondition of each risky operator in words, then create one example that satisfies it and one that violates it."),
          runStep("4. Control precision", "Keep full precision internally, choose round, math.floor, or math.ceil from the required guarantee, and format only at the display boundary.", "Raw, nearest, lower-bound, upper-bound, and displayed values compared deliberately.", "Each operation answers a different question: nearest value, no-more-than bound, enough-to-cover bound, or visible text. Substituting one for another can change capacity and threshold decisions.", "For one positive fraction and one negative fraction, write a table of round(value), math.floor(value), math.ceil(value), int(value), and a formatted label; compare both the values and types."),
          runStep("5. Stress the edges", "Test an exact integer, values immediately below and above it, and negative counterparts.", "Expected results explained from the mathematical model and number-line direction.", "Signs, exact divisibility, and thresholds reveal floor, ceiling, remainder, and comparison semantics that ordinary positive examples hide.", "Use values such as 2.0, 2.01, -2.0, and -2.01. If a calculated value only appears to be an integer, inspect repr(value) before applying a bound; never subtract an arbitrary epsilon without a domain rule."),
        ],
      },

      py03: {
        title: "Flow, Conditionals & Iteration",
        subtitle: "Control which statements run, how often they run, and what remains true during repetition.",
        objectives: [
          "Design branches with explicit priority.",
          "State and preserve loop invariants.",
          "Validate inputs and reason about termination.",
        ],
        tutorial: [
          section(
            "Turn a decision table into branches",
            "Conditionals select one path from several possibilities. Before coding, write rows containing a condition and its outcome, then order those rows from exceptional or narrow cases to the general case. In an if/elif/else chain, Python stops at the first true condition, so overlap and order are part of the behaviour rather than formatting choices.",
            "parcel_weight = 7.5\nif parcel_weight <= 0:\n    shipping = \"invalid\"\nelif parcel_weight <= 2:\n    shipping = \"letter rate\"\nelif parcel_weight <= 10:\n    shipping = \"standard rate\"\nelse:\n    shipping = \"freight rate\"\nprint(shipping)",
            [
              "Write every case and outcome before the if statement.",
              "Mark overlapping conditions and decide which has priority.",
              "Test just below, exactly at, and just above each boundary.",
            ],
            "Mental model: an elif chain is a top-to-bottom gate; only the first open gate is entered.",
            "Several independent if statements can run several outcomes when the rules were intended to be mutually exclusive.",
          ),
          section(
            "Give every loop state, progress, and a stop",
            "A while loop repeatedly checks its condition before entering the body. Describe what each control variable means at the top of an iteration, identify the update that changes it, and explain why that update eventually makes the condition false. A short trace table with iteration, state before, action, and state after is often enough to expose a missing update.",
            "crates_remaining = 12\ntruck_capacity = 5\nwhile crates_remaining > 0:\n    loaded_now = min(truck_capacity, crates_remaining)\n    print(\"loading\", loaded_now)\n    crates_remaining -= loaded_now",
            [
              "State the loop condition in plain language.",
              "Point to a state change that guarantees progress on every path.",
              "Trace the first iteration and the final partial iteration.",
            ],
            "Loop proof: the state stays valid, a measure moves toward its bound, and the bound eventually stops repetition.",
            "If continue skips the progress update, an apparently reasonable loop can repeat the same state forever.",
          ),
          section(
            "Accumulators remember processed work",
            "A for loop visits source items one at a time. An accumulator summarizes everything seen so far: an output list stores accepted transformations, a number stores a total, and a boolean stores a claim that remains true. Choose an initial value that correctly represents processing zero items, then state the invariant after each append or update.",
            "temperatures = [18, 21, 17, 24]\nwarmer_readings = []\nfor reading in temperatures:\n    if reading >= 20:\n        warmer_readings.append(reading)\nprint(warmer_readings)",
            [
              "Describe what the accumulator contains after k source items.",
              "Initialize it once, before the loop.",
              "Update it only when the current item satisfies the stated rule.",
            ],
            "Mental model: after each iteration, the accumulator is the complete answer for the prefix already visited.",
            "Appending to the source while iterating over it can extend the work unexpectedly; accumulate into a separate result.",
          ),
          section(
            "Control nested search with decisive evidence",
            "Nested loops are appropriate when each outer item owns a separate inner search. Reset inner state for every new outer item. If one match completely answers the current search, break only the inner loop and preserve a flag or result that explains what was found. Validate the overall shape before entering expensive repeated work.",
            "seats = [[\"taken\", \"taken\"], [\"taken\", \"open\"]]\nfirst_open = None\nfor row_index, row in enumerate(seats):\n    for seat_index, state in enumerate(row):\n        if state == \"open\":\n            first_open = (row_index, seat_index)\n            break\n    if first_open is not None:\n        break\nprint(first_open)",
            [
              "Give the outer and inner loops different, explicit responsibilities.",
              "Reset per-row or per-candidate state at the correct level.",
              "Use break only when later work cannot improve the required answer.",
            ],
            "Structure search around evidence: continue while evidence is incomplete and stop once it is decisive.",
            "A break exits only its nearest loop; assuming it exits every nesting level produces subtle extra work or a replaced result.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "Control flow as a state machine",
            body: "At any moment a program has state, observes a condition, chooses a transition, and arrives at new state. A branch chooses once; a loop repeats transitions while a guard remains true. Correctness comes from covering every state category, preserving an invariant, and ensuring some bounded measure moves toward a stopping state.",
            steps: [
              { label: "Partition", detail: "Divide the input domain into non-overlapping cases and give each case one intended outcome." },
              { label: "Prioritize", detail: "Order overlapping checks from exceptional or narrow cases to general fallbacks." },
              { label: "Initialize", detail: "Choose loop state whose meaning is already true before the first condition check." },
              { label: "Advance", detail: "Update a finite measure on every repeated path while preserving the invariant." },
              { label: "Stop", detail: "Describe the exact state that makes the guard false or justifies an early exit." },
            ],
          },
          guidedPractice: [
            {
              title: "Audit a temperature policy",
              prompt: "Design a decision table for four descriptive temperature bands, including exact boundary values, before writing any conditional code.",
              starterCode: "temperature = 18\n# Write ordered conditions only after the decision table.\n# description = ...",
              questions: [
                "Do adjacent bands overlap or leave a gap?",
                "Which condition must be checked first if one range is an exception inside another?",
                "Which three values best probe each boundary?",
              ],
              reveal: "A useful table lists a predicate, outcome, and representative boundary values for every row. Translate the rows in priority order into one mutually exclusive if/elif/else chain.",
            },
            {
              title: "Trace a draining counter",
              prompt: "Model a container that removes at most four units per iteration until empty. Trace state before and after each pass; do not copy the tutorial implementation.",
              starterCode: "remaining = 11\ncapacity = 4\n# while ...:\n#     removed_now = ...\n#     update remaining",
              questions: [
                "What does remaining mean at the top of every iteration?",
                "Which expression guarantees it never becomes invalid?",
                "Why must the update occur even on the final partial iteration?",
              ],
              reveal: "Use remaining as both state and a decreasing termination measure. Each iteration chooses a legal amount no larger than the state, then strictly reduces that state; trace 11, 7, 3, and the stopping value.",
            },
          ],
          glossary: [
            { term: "predicate", definition: "An expression evaluated for truth to guide a branch or repetition." },
            { term: "branch", definition: "One conditionally selected path through a program." },
            { term: "invariant", definition: "A claim that is true before and after every loop iteration." },
            { term: "accumulator", definition: "State summarizing all items or iterations processed so far." },
            { term: "termination measure", definition: "A bounded quantity that strictly moves toward a stopping condition." },
            { term: "short-circuiting", definition: "Stopping boolean evaluation as soon as the result is already determined." },
          ],
          debugChecklist: [
            "List all intended paths and confirm each input category reaches exactly one of them.",
            "Test just below, exactly at, and just above every comparison boundary.",
            "Write a loop trace with state before, chosen action, and state after each iteration.",
            "Confirm every continue path still performs the required progress update.",
            "Check the invariant before the first iteration and after the final one.",
            "Use a deliberately small input so an extra or missing iteration is visible.",
          ],
          checkpoint: {
            question: "Why can several independent if statements behave differently from one if/elif/else chain?",
            options: ["Independent if statements cannot use comparisons", "Every true independent if may run, while a chain selects only the first true branch", "elif conditions run before if", "A chain repeats until all conditions are false"],
            answerIndex: 1,
            explanation: "Independent conditions are all evaluated. An if/elif/else chain encodes priority and commits to the first true path.",
          },
        },
        coachConversation: [
          { learner: "My conditions all seem true for one value. Which branch wins?", coach: "Write the overlapping cases in a decision table first. An if/elif/else chain chooses the first true branch, so put the most specific or highest-priority rule before a broader one." },
          { learner: "The loop never stops. Where should I look?", coach: "Name the changing state and a finite measure that moves toward the stop condition. Check every branch, especially continue paths, to prove that the measure still changes." },
          { learner: "Can I read a number directly in the condition?", coach: "Yes when valid numeric text is guaranteed: age = int(input()) creates an integer before comparison. If invalid text is part of the domain, keep the raw string and handle the possible ValueError deliberately rather than hiding it." },
        ],
        documentation: [
          { label: "if statements", description: "Branch structure, elif ordering, and the optional else branch.", url: "https://docs.python.org/3/tutorial/controlflow.html#if-statements" },
          { label: "for statements", description: "How Python visits each item from an iterable in sequence.", url: "https://docs.python.org/3/tutorial/controlflow.html#for-statements" },
          { label: "The range() function", description: "How integer ranges define loop starts, excluded stops, and steps.", url: "https://docs.python.org/3/tutorial/controlflow.html#the-range-function" },
          { label: "break and continue", description: "Ending a loop early or advancing deliberately to the next iteration.", url: "https://docs.python.org/3/tutorial/controlflow.html#break-and-continue-statements" },
        ],
        runbook: [
          runStep("1. Enumerate paths", "Build a table of conditions, outcomes, overlaps, and priority before writing branches.", "One representative and one boundary value per row.", "A decision table exposes gaps and overlaps before source order silently assigns them behaviour.", "Draw the number line or input categories first; colour each region with its intended outcome, then convert regions to predicates."),
          runStep("2. Define loop state", "Explain every control variable and accumulator at the top of an iteration.", "A one-sentence invariant for each loop level.", "Without stable meanings, updates become arbitrary and nested loops easily mix responsibilities.", "Pause before the body and finish the sentence: before iteration k, this name equals or contains...."),
          runStep("3. Prove progress", "Show that every repeated path changes a finite measure toward termination.", "A three-row trace plus the final state.", "A true guard alone does not guarantee completion; a decreasing or increasing bounded measure does.", "Circle each continue and branch, then point to the update performed on that path; missing progress identifies the likely infinite loop."),
          runStep("4. Place exits", "Decide where validation, continue, break, and early results are justified by decisive evidence.", "Each exit annotated with why later work is unnecessary.", "Early exits are clearest when they follow a proof that no remaining work can change the result.", "Temporarily replace the exit with a comment stating the decisive fact; if you cannot finish the sentence, the exit may be premature."),
          runStep("5. Cover boundaries", "Exercise every branch, empty or smallest ranges, and the exact final iteration.", "Expected and observed state transitions, not only final output.", "Control-flow defects often preserve the final answer for ordinary cases while doing one wrong transition at an edge.", "Use a trace table and compare iteration counts as well as outputs; start with the smallest input that should enter the loop once."),
        ],
      },

      py04: {
        title: "Functions",
        subtitle: "Build small, reusable contracts with explicit inputs, outputs, and failure behaviour.",
        objectives: [
          "Separate calculation from display and input.",
          "Decompose algorithms into focused helpers.",
          "Make every return path satisfy one stable contract.",
        ],
        tutorial: [
          section(
            "Start from a precise callable contract",
            "A function contract names accepted inputs, the meaning and type of the return value, side effects, and behaviour at the boundaries. Write that promise before the body. The implementation may change, but callers should not need to know how the result is produced. A useful docstring describes the semantic rule rather than narrating individual lines.",
            "def clamp(value, lower, upper):\n    \"\"\"Return value constrained to the inclusive interval.\"\"\"\n    if value < lower:\n        return lower\n    if value > upper:\n        return upper\n    return value",
            [
              "State each parameter's meaning and valid domain.",
              "State one stable return type and any sentinel explicitly.",
              "Say whether caller-owned data may be modified.",
            ],
            "Mental model: a function is a small machine with an input socket, an output socket, and an explicit promise between them.",
            "A branch with no return silently produces None, violating a contract even when the popular path works.",
          ),
          section(
            "Return data; display it at the boundary",
            "return hands a value back to the caller and immediately ends that function call. print writes text to stdout and itself returns None. Keeping calculation functions free of display lets other code test, compare, store, or format the returned value. A command-line wrapper may print the result, but that is a separate responsibility.",
            "def rectangle_perimeter(width, height):\n    return 2 * (width + height)\n\nperimeter = rectangle_perimeter(8, 3)\nprint(f\"Border needed: {perimeter} m\")",
            [
              "Use return for the value promised by the contract.",
              "Keep labels and presentation outside the calculation function.",
              "Assert both the returned value and its type.",
            ],
            "Pure core, visible shell: compute reusable data inside and format it only where people or files are involved.",
            "A function that prints the right-looking answer may still give its caller only None.",
          ),
          section(
            "Use helpers to name separate ideas",
            "Extract a helper when a transformation has a clear name, an independent contract, or more than one caller. This lowers the number of ideas held in mind at once. The public function should read like a short explanation that delegates details, while each helper remains small enough to test with a few focused examples.",
            "def hexadecimal_channel(pair):\n    return int(pair, 16)\n\ndef colour_brightness(hex_colour):\n    red = hexadecimal_channel(hex_colour[1:3])\n    green = hexadecimal_channel(hex_colour[3:5])\n    blue = hexadecimal_channel(hex_colour[5:7])\n    return round((red + green + blue) / 3)",
            [
              "Name the concept the helper owns in a verb or noun phrase.",
              "Pass required data explicitly instead of reading distant globals.",
              "Test the helper before composing it into the public function.",
            ],
            "Good decomposition makes the function names read like the vocabulary of the explanation.",
            "Extracting arbitrary chunks merely to shorten a function creates helpers with unclear contracts and hidden coupling.",
          ),
          section(
            "Guard assumptions, then test from the outside",
            "A guard clause handles an invalid, empty, or already-complete case before the main path. This keeps the normal flow shallow and makes failure behaviour explicit. Black-box tests call only the public function and verify its contract with ordinary values, each boundary, and inputs that activate every guard.",
            "def safe_ratio(numerator, denominator):\n    if not isinstance(numerator, (int, float)):\n        return None\n    if not isinstance(denominator, (int, float)) or denominator == 0:\n        return None\n    return numerator / denominator\n\nassert safe_ratio(9, 3) == 3\nassert safe_ratio(9, 0) is None",
            [
              "Check assumptions before operations that depend on them.",
              "Give every guard a documented return value or exception.",
              "Test one case through every return path.",
            ],
            "A guard protects the core; a black-box test proves the caller-visible promise.",
            "A broad except clause can turn genuine coding defects into misleading fallback values; guard known conditions deliberately.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "A function is a promise with a private mechanism",
            body: "Callers should reason from a function's contract rather than its body. Parameters carry explicit dependencies in, return carries one stable kind of result out, and side effects must be declared. Decomposition works when helper names describe ideas in the solution, not merely chunks of lines.",
            steps: [
              { label: "Promise", detail: "Name parameter meanings, accepted domain, return meaning and type, and mutation or failure policy." },
              { label: "Guard", detail: "Handle inputs that cannot follow the normal path before performing dependent operations." },
              { label: "Delegate", detail: "Extract independently explainable rules into helpers with smaller contracts." },
              { label: "Compose", detail: "Pass returned values explicitly so the public function reads like the domain explanation." },
              { label: "Observe", detail: "Test only caller-visible values, exceptions, and agreed side effects rather than implementation details." },
            ],
          },
          guidedPractice: [
            {
              title: "Specify a label normalizer",
              prompt: "Write only the contract and test cases for a function that receives a text label and returns a cleaned display label. Do not implement the body yet.",
              starterCode: "def normalize_label(raw_label):\n    \"\"\"TODO: document accepted input and returned text.\"\"\"\n    pass\n\n# Add assertions for ordinary, padded, and empty text.",
              questions: [
                "Will empty text return an empty string, a sentinel, or raise an exception?",
                "Does the function mutate anything owned by the caller?",
                "Which aspects of capitalization and whitespace are part of the promise?",
              ],
              reveal: "A complete contract removes ambiguity before code: one accepted input type, a documented empty-input policy, one stable text return type, and no hidden output. Tests should describe that public promise without relying on local variables inside the function.",
            },
            {
              title: "Separate geometry from presentation",
              prompt: "Design a reusable function that derives the area of a rectangle and a separate shell that turns the returned value into a human-readable sentence.",
              starterCode: "def rectangle_area(width, height):\n    # Return reusable data; do not print here.\n    pass\n\n# The outer shell may format and display the result.",
              questions: [
                "What should the core return, and what type should it always have?",
                "Where should units and friendly labels be added?",
                "Which boundary inputs deserve explicit tests?",
              ],
              reveal: "Keep the core numeric and deterministic so a caller can compare or reuse the result. The shell owns labels and output; test the core with positive, zero, and policy-relevant negative dimensions.",
            },
          ],
          glossary: [
            { term: "parameter", definition: "A name in a function definition that receives an argument for one call." },
            { term: "argument", definition: "A value supplied by the caller for a parameter." },
            { term: "return value", definition: "The value handed back to the caller when a function completes a return statement." },
            { term: "contract", definition: "The documented agreement covering inputs, outputs, failures, and side effects." },
            { term: "guard clause", definition: "An early check that handles a special or invalid case before the normal path." },
            { term: "side effect", definition: "An observable change beyond returning a value, such as printing or mutation." },
          ],
          debugChecklist: [
            "Call the function with a minimal example and inspect both the value and type returned.",
            "Check every conditional path for an explicit return consistent with the contract.",
            "Distinguish a printed value from a returned value; print itself returns None.",
            "Test helpers independently before investigating their composition.",
            "Snapshot mutable arguments before and after the call to detect accidental mutation.",
            "Confirm that guard clauses handle only documented cases and do not hide programming errors.",
          ],
          checkpoint: {
            question: "A function prints 42 but has no return statement. What value does its caller receive?",
            options: ["42", "The string \"42\"", "None", "No value can be assigned"],
            answerIndex: 2,
            explanation: "Display and return are separate channels. Without an explicit return value, Python completes the call with None.",
          },
        },
        coachConversation: [
          { learner: "Should this function call input() itself?", coach: "Usually keep input and print in a thin outer layer. Parse text there—for example, quantity = int(input())—and pass the integer into a function whose contract is easy to call and test." },
          { learner: "I can see the right value, so why did the caller receive None?", coach: "print(value) displays a value but returns None. Follow every function path and use return when the contract promises data back to the caller." },
          { learner: "When is a helper worth creating?", coach: "Extract a helper when you can give one transformation a clear name, parameters, and return type. If the helper only forwards arguments without clarifying an idea, keep the flow together." },
          { learner: "Should I write import module or from module import name?", coach: "Prefer import module when the qualified name clarifies where a tool comes from, such as math.isclose. A focused from-import can be concise when the source remains obvious. Avoid import * because it hides name origins and can silently overwrite another binding." },
        ],
        documentation: [
          { label: "Defining functions", description: "Parameters, local scope, default values, and function definitions in the Python tutorial.", url: "https://docs.python.org/3/tutorial/controlflow.html#defining-functions" },
          { label: "return statement", description: "How return exits a function and supplies a value to its caller.", url: "https://docs.python.org/3/reference/simple_stmts.html#the-return-statement" },
          { label: "Built-in functions", description: "Reference for reusable built-ins and their callable contracts.", url: "https://docs.python.org/3/library/functions.html" },
        ],
        runbook: [
          runStep("1. Specify the promise", "Write parameter meanings, valid domains, return type, sentinel behaviour, and mutation policy.", "A docstring plus several input → return examples.", "A stable contract lets callers and tests agree on behaviour independently of the chosen implementation.", "Write three calls first—ordinary, boundary, and invalid—and decide their exact returned values and types before the body."),
          runStep("2. Decompose by idea", "Name independently explainable transformations before extracting helpers.", "A dependency sketch with one responsibility per function.", "Concept-based helpers reduce mental load and can be verified without constructing the entire workflow.", "Explain the algorithm aloud; phrases with distinct verbs or domain nouns are candidates for helpers, while arbitrary line ranges are not."),
          runStep("3. Guard the boundary", "Handle invalid, empty, and already-complete states before the normal path.", "One explicit, documented outcome per guard.", "Guards prevent unsafe operations and leave the main path focused on inputs that satisfy its assumptions.", "List each operation that can fail, write its required condition, and place the narrowest deliberate check immediately before the core."),
          runStep("4. Compose and return", "Pass values between helpers and ensure every path returns the promised type.", "No display logic or accidental fall-through in the core.", "Explicit value flow makes dependencies inspectable and prevents stdout or globals from becoming a hidden communication channel.", "Assign each helper return to a descriptive name and inspect its type; search every branch for a missing return or unexpected None."),
          runStep("5. Test as a caller", "Exercise normal, boundary, invalid, and side-effect cases through the public function.", "Black-box assertions plus input snapshots when mutation matters.", "Caller-focused tests survive refactoring because they verify the promise rather than private implementation steps.", "Hide the body while writing assertions; if a test requires a local name, restate it in terms of returned data or documented effects."),
        ],
      },

      py05: {
        title: "Strings & Tuples",
        subtitle: "Transform immutable sequences through indexes, parsing, and deliberate reconstruction.",
        objectives: [
          "Use indexing and slicing without off-by-one errors.",
          "Separate parsing, transformation, and formatting.",
          "Traverse flat and nested immutable data safely.",
        ],
        tutorial: [
          section(
            "Think of indexes as positions and slices as gaps",
            "A string index selects one character: zero is the first position and -1 is the final position. A slice selects the region between two gaps, includes its start, and excludes its stop. Drawing characters above indexes and gaps prevents most off-by-one mistakes. Because strings and tuples are immutable, every slice and concatenation creates a new value rather than editing the original.",
            "filename = \"field-notes.csv\"\nextension = filename[-4:]\nstem = filename[:-4]\nalternate_letters = stem[::2]\nprint(stem, extension, alternate_letters)",
            [
              "Draw positive and negative indexes on a sample sequence.",
              "Describe each slice in words before running it.",
              "Check length before using a fixed index whose existence is not guaranteed.",
            ],
            "Mental model: indexes point at elements; slice boundaries point at the gaps around them.",
            "A negative slice boundary is still calculated from the end; on short inputs it may select a surprising but valid empty region instead of raising.",
          ),
          section(
            "Parse structure before transforming text",
            "Delimiters encode structure inside a flat string. split turns that external representation into meaningful fields, a middle stage cleans or transforms each field, and join creates the final representation once. Decide whether empty fields are valid and whether surrounding whitespace belongs to data before choosing the cleanup rule.",
            "raw_tags = \" urgent | backend | needs review \"\ntags = raw_tags.split(\"|\")\nclean_tags = [tag.strip().upper() for tag in tags]\nlabel = \" / \".join(clean_tags)\nprint(label)",
            [
              "Write down the delimiter and policy for empty pieces.",
              "Transform one representative field before mapping the rule to all fields.",
              "Choose the final separator and join exactly once.",
            ],
            "Mental model: parse expands text into structure; format compresses structure back into text.",
            "Calling strip on the entire string removes only its outer whitespace, not whitespace around every internal field.",
          ),
          section(
            "Build transformed text without mutating it",
            "Since a string cannot be changed in place, visit its characters and append transformed pieces to a separate list, then join once. Each branch should decide the replacement for exactly one character. This makes preservation rules explicit: characters not selected for transformation should normally pass through unchanged.",
            "announcement = \"Gate A opens soon\"\npieces = []\nfor character in announcement:\n    if character.lower() in \"aeiou\":\n        pieces.append(\"*\")\n    else:\n        pieces.append(character)\nmasked = \"\".join(pieces)\nprint(masked)",
            [
              "Define the one-character transformation rule in plain language.",
              "Initialize the output builder once before traversal.",
              "Test uppercase, lowercase, spaces, and punctuation.",
            ],
            "Builder pattern: decide one output piece per input piece, then assemble the immutable result once.",
            "Repeatedly concatenating a long string inside a loop creates many temporary strings; collecting pieces and joining is clearer and scales better.",
          ),
          section(
            "Use tuple shape as part of the model",
            "A tuple communicates a fixed record or an immutable grouping. Position has meaning, so unpack fields into names as soon as their shape is known. Nested tuples express records containing grouped subvalues; document that shape and keep the return shape stable. Use indexing for genuinely positional data and named structures when fields become numerous or ambiguous.",
            "book = (\"The Left Hand of Darkness\", 1969, (\"science fiction\", \"classic\"))\ntitle, year, categories = book\nprimary_category, secondary_category = categories\nprint(title)\nprint(year, primary_category, secondary_category)",
            [
              "Write the expected tuple shape before unpacking.",
              "Use descriptive names for positional fields.",
              "Test an empty nested group and reject malformed shapes deliberately.",
            ],
            "Mental model: a tuple is a small fixed schema whose positions carry meaning.",
            "Packing unrelated values into a long tuple makes positional mistakes easy and leaves callers guessing what each index means.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "Immutable sequences are maps of positions",
            body: "Strings and tuples preserve order but cannot be edited in place. Indexes identify elements, slice boundaries identify gaps, and tuple positions can form a small schema. Transformation therefore means reading a known shape, producing pieces, and constructing a new immutable value.",
            steps: [
              { label: "Map", detail: "Draw elements, positive and negative indexes, and the gaps that a slice can use as boundaries." },
              { label: "Validate", detail: "Confirm the sequence is long enough or structurally valid before fixed indexing or unpacking." },
              { label: "Parse", detail: "Convert encoded text or positional records into named intermediate fields once." },
              { label: "Transform", detail: "Decide the output piece for each selected element without changing the source." },
              { label: "Assemble", detail: "Join text pieces or pack tuple fields once in the required order and shape." },
            ],
          },
          guidedPractice: [
            {
              title: "Annotate a filename",
              prompt: "For the unrelated text archive-2026.log, draw indexes and gaps, then plan how to separate the stem and suffix without writing a complete exercise solution.",
              starterCode: "filename = \"archive-2026.log\"\n# Draw the final four character positions first.\n# stem = filename[...]\n# suffix = filename[...]",
              questions: [
                "Which boundary belongs to both complementary slices?",
                "What happens when the text is shorter than the expected suffix?",
                "Why does slicing leave filename unchanged?",
              ],
              reveal: "Complementary half-open slices share one gap: everything before it and everything from it onward. Decide a policy for short or malformed names before treating a fixed suffix length as guaranteed.",
            },
            {
              title: "Parse a compact route record",
              prompt: "Turn a route record such as north|17|active into three named fields, convert only the count, and plan a different display representation.",
              starterCode: "raw_record = \"north|17|active\"\n# fields = ...\n# direction, count_text, status = ...\n# convert count_text and construct a new value",
              questions: [
                "What exact tuple-like shape do you expect after splitting?",
                "How should a missing or extra delimiter be handled?",
                "Why is parsing once clearer than repeatedly splitting the original text?",
              ],
              reveal: "Split at the known delimiter once, validate the field count, unpack into semantic names, and convert only the numeric field. Formatting is a later stage that should operate on this structured representation.",
            },
          ],
          glossary: [
            { term: "immutable", definition: "Unable to change after creation; transformations produce another object." },
            { term: "index", definition: "An integer position selecting one sequence element." },
            { term: "slice", definition: "A half-open selection described by start, stop, and optional step." },
            { term: "delimiter", definition: "A character or substring marking boundaries between encoded fields." },
            { term: "unpacking", definition: "Binding the elements of a known iterable shape to several names at once." },
            { term: "join", definition: "Construction of one string by placing a separator between string pieces." },
          ],
          debugChecklist: [
            "Draw the sequence and label both element indexes and slice gaps.",
            "Check length before any fixed index whose existence is not guaranteed.",
            "Inspect split fields with repr so empty fields and whitespace remain visible.",
            "Verify that unpacking expects exactly the produced number and nesting of fields.",
            "Test empty, one-character, delimiter-leading, and delimiter-trailing text.",
            "Compare the final value and original input to confirm reconstruction did not imply mutation.",
          ],
          checkpoint: {
            question: "For text[2:5], which character positions are selected?",
            options: ["2, 3, 4", "2, 3, 4, 5", "3, 4, 5", "Only 2 and 5"],
            answerIndex: 0,
            explanation: "Python slices include the start position and exclude the stop position, so the selected indexes are 2 through 4.",
          },
        },
        coachConversation: [
          { learner: "I keep missing one character in a slice. How should I picture it?", coach: "Label element indexes, then draw slice boundaries in the gaps. text[start:stop] includes start and excludes stop, so the width of a forward slice is stop - start." },
          { learner: "When should numeric-looking text stay a string?", coach: "Keep it as str when it is an identifier, code, or formatted token—leading zeroes may matter. Use int(text) only when the meaning is a whole quantity that needs arithmetic." },
          { learner: "Why can I not change one character or tuple item?", coach: "Strings and tuples are immutable. Build new pieces and join or pack them once; also check length and tuple shape before indexing or unpacking." },
        ],
        documentation: [
          { label: "Text sequence type: str", description: "String construction, immutability, and text operations.", url: "https://docs.python.org/3/library/stdtypes.html#text-sequence-type-str" },
          { label: "Common sequence operations", description: "Indexing, slicing, membership, concatenation, and sequence length.", url: "https://docs.python.org/3/library/stdtypes.html#common-sequence-operations" },
          { label: "String methods", description: "Official reference for split, strip, join, replace, case operations, and more.", url: "https://docs.python.org/3/library/stdtypes.html#string-methods" },
          { label: "Tuples and sequences", description: "Tuple packing, unpacking, and sequence use in the Python tutorial.", url: "https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences" },
        ],
        runbook: [
          runStep("1. Draw the representation", "Mark indexes, slice gaps, delimiters, and nested tuple shape on a non-exercise example.", "An annotated sequence or record diagram.", "A concrete positional model turns off-by-one and shape errors into visible inconsistencies before execution.", "Use a five-character toy string and write indexes above, negative indexes below, and vertical bars at every slice gap."),
          runStep("2. Define edge policy", "Decide behaviour for empty, short, malformed, and delimiter-heavy input before indexing.", "A small edge-case table with stable return types.", "Immutable operations are often permissive—an odd slice may return empty—so domain validation cannot be delegated to Python syntax.", "For each assumption, invent the smallest input that violates it and choose a documented return or exception."),
          runStep("3. Parse once", "Turn external text into fields or unpack a fixed tuple into meaningful names.", "Structured intermediate data whose shape is documented.", "A single parsing boundary prevents representational details from leaking into every transformation step.", "Print repr of the intermediate fields and their count; resolve whitespace and empty-field policy there before continuing."),
          runStep("4. Construct anew", "Traverse immutable input and collect output pieces without trying to mutate it.", "A new string or tuple with the original unchanged.", "Construction matches the ownership model and makes the preservation rule for unmodified elements explicit.", "For one source element, answer what exact output piece it contributes; append those pieces to a builder and join once."),
          runStep("5. Format exactly", "Join or pack the result once and verify separators, case, whitespace, and order.", "Character-accurate output plus an original-input snapshot.", "Formatting is part of the public representation, and repeated ad hoc concatenation makes separators and whitespace inconsistent.", "Show the final text with repr and compare its length, separators, and first/last characters with a hand-built expectation."),
        ],
      },

      py06: {
        title: "Lists",
        subtitle: "Use mutable sequences intentionally, with clear ownership and index discipline.",
        objectives: [
          "Choose when to mutate, copy, or construct a new list.",
          "Reason about indexes after insertions and removals.",
          "Apply stacks, matrices, and circular indexing.",
        ],
        tutorial: [
          section(
            "Decide who owns a mutable list",
            "Assignment copies a reference, not the list object. If two names reference the same list, append, pop, item assignment, and other mutation through either name are visible through both. Make a shallow copy when the outer list must be independent; nested objects remain shared unless they are copied too. Treat the choice between mutation and a new result as part of the function contract.",
            "shopping = [\"rice\", \"beans\"]\nplanned = shopping.copy()\nplanned.append(\"lemons\")\nprint(shopping)\nprint(planned)",
            [
              "State whether callers should observe mutation.",
              "Use is to check identity and == to check equal contents.",
              "Snapshot the original before and after a transformation test.",
            ],
            "Mental model: a name is a label pointing to an object; mutation changes the object, not merely the label.",
            "A shallow copy duplicates only the outer container, so editing an inner list can still change data visible from the original.",
          ),
          section(
            "Partition sequences with slice boundaries",
            "List slices produce new outer lists and preserve element order. A boundary can separate a header from its payload, processed work from pending work, or fixed-size pages from remaining items. Compute the boundary once, name both partitions, and remember that the stop is exclusive. Concatenation creates another new list rather than extending either source.",
            "queue = [\"A17\", \"B04\", \"C22\", \"D11\", \"E09\"]\nbatch_size = 2\ncurrent_batch = queue[:batch_size]\nwaiting = queue[batch_size:]\nprint(current_batch)\nprint(waiting)",
            [
              "Describe what lies before and after the slice boundary.",
              "Test a boundary of zero, one, and greater than the list length.",
              "Decide whether nested element sharing is acceptable.",
            ],
            "Mental model: slicing draws a non-destructive boundary through an ordered collection.",
            "Assuming a slice deep-copies nested elements can lead to unexpected shared mutation later.",
          ),
          section(
            "Rebuild when membership changes during traversal",
            "Deleting an element shifts every later element one position left. A for loop over that same list may then skip the item that moved into the current position. When the goal is to keep items satisfying a rule, constructing a new list is usually safer. When in-place deletion is required, traverse indexes backward or control the index explicitly.",
            "tasks = [\n    {\"title\": \"backup\", \"expired\": False},\n    {\"title\": \"old report\", \"expired\": True},\n    {\"title\": \"deploy\", \"expired\": False},\n]\nactive_tasks = [task for task in tasks if not task[\"expired\"]]\nprint(active_tasks)",
            [
              "State whether the source must survive unchanged.",
              "Prefer a new result when selection is the real operation.",
              "If mutating, trace indexes after each deletion.",
            ],
            "After structural mutation, every saved later index describes the old shape and must be reconsidered.",
            "Calling remove deletes the first equal value, not necessarily the specific object or position the algorithm intended.",
          ),
          section(
            "Choose a list role and protect its invariant",
            "A list can be a sequence, output builder, stack, queue-like buffer, or grid. Name the role because it determines legal operations. In a stack, append pushes and pop removes the most recent item; its invariant concerns the history still available. For a grid, each nested list is a row and rectangular shape must be checked before column indexing.",
            "undo_stack = []\ndocument = \"draft\"\nundo_stack.append(document)\ndocument = \"revised draft\"\nundo_stack.append(document)\ndocument = \"final draft\"\nprevious_version = undo_stack.pop()\nprint(previous_version)",
            [
              "Name whether the list represents order, history, output, or coordinates.",
              "State what the final element means before using pop.",
              "Check non-empty state before reading or removing the top item.",
            ],
            "Mental model: the data structure's role is a promise about what every position means after each operation.",
            "Using pop(0) repeatedly as a queue shifts all later items each time; collections.deque is better for large front removals.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "Lists combine order, identity, and ownership",
            body: "A list is not only a row of values; it is a mutable object that names may share. First decide who owns it and whether mutation is visible, then choose its structural role—sequence, builder, stack, queue, or grid. Every structural edit changes the index map and may invalidate earlier positional reasoning.",
            steps: [
              { label: "Own", detail: "State whether the operation mutates caller data, makes a shallow copy, or constructs a fully independent result." },
              { label: "Model", detail: "Give the collection one role and define what each index or nested coordinate means." },
              { label: "Change", detail: "Apply structural operations with awareness that later positions may shift." },
              { label: "Inspect", detail: "Compare equality, identity, and nested sharing rather than treating them as the same question." },
              { label: "Challenge", detail: "Probe empty, duplicate, nested, and ragged shapes according to the declared contract." },
            ],
          },
          guidedPractice: [
            {
              title: "Plan two independent shopping lists",
              prompt: "Start with a base shopping list and make a planned variant that can be extended without changing the base. Explain the identity relationship before coding.",
              starterCode: "base = [\"oats\", \"tea\"]\n# planned = ...\n# add one item only to planned\n# compare base == planned and base is planned",
              questions: [
                "Why does planned = base not create a second list?",
                "Which copy depth is sufficient when every element is an immutable string?",
                "How would nested lists change that answer?",
              ],
              reveal: "The two names must point to different outer list objects. A shallow copy is enough for immutable elements, but nested mutable elements remain shared and require a deliberate deeper ownership policy.",
            },
            {
              title: "Trace a tiny queue",
              prompt: "Model three work items entering an ordered queue and one item leaving from the front. Draw indexes before and after the removal.",
              starterCode: "queue = [\"scan\", \"index\", \"publish\"]\n# remove the next item according to queue semantics\n# record remaining order",
              questions: [
                "Which end represents arrival and which represents service?",
                "What becomes index zero after one removal?",
                "When would collections.deque be a more scalable representation?",
              ],
              reveal: "Queue semantics are first-in, first-out, so the oldest element leaves and every remaining list index shifts. A list is understandable for small practice; repeated front removal motivates deque in larger systems.",
            },
          ],
          glossary: [
            { term: "mutable", definition: "Able to change in place while retaining the same object identity." },
            { term: "alias", definition: "Another name or reference pointing to the same object." },
            { term: "shallow copy", definition: "A new outer container whose element references are shared with the source." },
            { term: "in-place operation", definition: "An operation that changes an existing object rather than returning an independent replacement." },
            { term: "stack", definition: "A last-in, first-out collection commonly operated at one list end." },
            { term: "ragged list", definition: "A nested list whose inner rows have different lengths." },
          ],
          debugChecklist: [
            "Record id relationships with is and content relationships with == separately.",
            "Snapshot caller-owned input before a function call and compare it afterward.",
            "Redraw indexes after every insertion, deletion, or pop.",
            "Avoid deleting from a list while a forward for loop is traversing it.",
            "Check nested row lengths before assuming a rectangular matrix.",
            "Test duplicates when using remove, index, membership, or equality-based operations.",
          ],
          checkpoint: {
            question: "After b = a for a list a, what does b.append(x) do?",
            options: ["Changes only b", "Creates a copy of a, then changes it", "Changes the one list visible through both a and b", "Raises because assignment makes lists immutable"],
            answerIndex: 2,
            explanation: "Assignment copies the reference. Both names point at the same mutable list object until a new object is explicitly created.",
          },
        },
        coachConversation: [
          { learner: "Why did changing my new variable also change the original list?", coach: "Assignment creates another reference to the same list; it does not copy the elements. State the ownership rule first, then use a copy or construct a new result when caller data must stay unchanged." },
          { learner: "Can I remove items while a for loop walks the list?", coach: "That changes the indexes under the iterator and can skip values. Prefer a new list, a comprehension, or a carefully documented reverse/index-based strategy." },
          { learner: "I read a row of numbers with input(). What do I have?", coach: "You still have text. split() produces strings too, so convert deliberately—for example, values = [int(piece) for piece in input().split()]—only when the contract guarantees integer fields." },
        ],
        documentation: [
          { label: "More on lists", description: "List methods, stacks, queues, and important mutation behaviour.", url: "https://docs.python.org/3/tutorial/datastructures.html#more-on-lists" },
          { label: "List comprehensions", description: "Readable construction of new lists from transformations and filters.", url: "https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions" },
          { label: "Sequence operations", description: "Shared indexing, slicing, concatenation, repetition, and membership rules.", url: "https://docs.python.org/3/library/stdtypes.html#common-sequence-operations" },
          { label: "Copy operations", description: "The difference between shallow and deep copying for nested mutable data.", url: "https://docs.python.org/3/library/copy.html" },
        ],
        runbook: [
          runStep("1. Declare ownership", "Decide whether caller input may change and whether nested elements are shared.", "An explicit in-place, shallow-copy, or new-result policy.", "Mutation is caller-visible behaviour, so an unstated ownership choice can make a locally correct result corrupt later work.", "Draw names as arrows to objects; if two arrows reach one list, decide whether that sharing is intentional before any append or assignment."),
          runStep("2. Name the role", "Identify the list as ordered data, builder, stack, queue, or grid.", "A one-sentence invariant for every position used.", "A collection role determines legal operations and gives positions stable meaning beyond convenient syntax.", "Choose one noun for the structure and describe what the first, current, and final positions mean; mixed answers suggest mixed responsibilities."),
          runStep("3. Trace structural edits", "Draw indexes before and after each append, pop, insert, slice, or deletion.", "A small transition table that exposes shifting positions.", "Structural mutation changes the coordinate system, so stored indexes and forward traversal may no longer reference the intended items.", "Use a three-element list and perform one operation by hand; then update every index before reasoning about the next line."),
          runStep("4. Challenge the shape", "Test empty, one-item, duplicate, nested, and ragged inputs when permitted.", "Shape-specific expected outcomes and guarded failures.", "List algorithms frequently depend on length, uniqueness, or rectangular nesting even when those assumptions are not written down.", "Turn each shape assumption into an assertion or guard, then create the smallest counterexample to determine the intended policy."),
          runStep("5. Verify aliasing", "Compare identities, values, and caller snapshots after the operation.", "Evidence that mutation and sharing match the declared policy.", "Equal contents do not prove independent ownership, and different outer objects do not prove nested independence.", "Check outer identity first, then mutate one nested test element deliberately and observe whether the corresponding source element changes."),
        ],
      },

      py07: {
        title: "Dictionaries & Sets",
        subtitle: "Model identity, frequency, membership, and relationships with hash-based collections.",
        objectives: [
          "Choose stable dictionary keys and dependable defaults.",
          "Use set algebra for membership questions.",
          "Handle ties, missing keys, mutation, and ordering explicitly.",
        ],
        tutorial: [
          section(
            "Design keys around the lookup question",
            "A dictionary maps each unique, hashable key to one value. Start by completing the sentence 'given this ___, I need that ___.' The first blank is usually the key. Keys should be stable identifiers; values may be simple facts or richer records. Assignment to an existing key replaces its value, while reading a missing key with brackets raises KeyError.",
            "stock_by_sku = {\"PEN-BLU\": 24, \"NOTE-A5\": 11}\nshipment = {\"PEN-BLU\": 8, \"ERASER-WHT\": 6}\nfor sku, quantity in shipment.items():\n    previous = stock_by_sku.get(sku, 0)\n    stock_by_sku[sku] = previous + quantity\nprint(stock_by_sku)",
            [
              "Write the lookup sentence before choosing the key.",
              "Choose an immutable, canonical identifier.",
              "Document the meaning and type of every stored value.",
            ],
            "Mental model: a dictionary is an index you design for a specific retrieval question.",
            "Using display names as keys can merge distinct records when names are not guaranteed unique.",
          ),
          section(
            "Group related records deliberately",
            "A dictionary value can itself be a list, set, or record. Grouping is a two-stage operation: obtain the collection for a key, then update that collection. setdefault can create the initial collection, but an explicit membership branch may be clearer while learning. Decide whether repeated values should be preserved, removed, or counted before choosing the nested type.",
            "books = [(\"Ada\", \"Algorithms\"), (\"Lin\", \"Networks\"), (\"Ada\", \"Compilers\")]\nbooks_by_reader = {}\nfor reader, title in books:\n    books_by_reader.setdefault(reader, []).append(title)\nprint(books_by_reader[\"Ada\"])",
            [
              "Choose list, set, or number from the required duplicate semantics.",
              "Initialize a fresh nested collection for each new key.",
              "Test multiple records for one key and a key with one record.",
            ],
            "Mental model: the outer dictionary finds a group; the nested value represents the group according to explicit rules.",
            "Using the same pre-created list as the default for several keys makes all those keys share one mutable collection.",
          ),
          section(
            "Translate membership rules into set algebra",
            "A set stores distinct hashable values and intentionally discards order and duplicate counts. Intersection answers what belongs to both groups, union answers what belongs to either, and difference answers what belongs to the left group but not the right. Draw a small Venn diagram first, then choose the operator matching the shaded region.",
            "requested = {\"read\", \"write\", \"deploy\"}\nrole_permissions = {\"read\", \"write\", \"comment\"}\ngranted = requested & role_permissions\ndenied = requested - role_permissions\nprint(\"granted:\", sorted(granted))\nprint(\"denied:\", sorted(denied))",
            [
              "Confirm that order and multiplicity do not carry meaning.",
              "Name the left and right operands in a directional difference.",
              "Sort only at the display boundary when deterministic order is required.",
            ],
            "Mental model: set operations calculate regions of a membership diagram.",
            "A - B and B - A answer different questions; treating difference as symmetric loses direction.",
          ),
          section(
            "Separate absence, falsey values, and mutation",
            "A missing key is different from a present key holding 0, False, an empty string, or None. Use key in mapping when presence itself matters and get when a documented fallback is appropriate. Copy caller-owned mappings before destructive cleanup. If output order is part of the contract, sort keys or items explicitly rather than relying on incidental construction order.",
            "settings = {\"theme\": \"night\", \"autosave\": False, \"legacy_mode\": True}\nworking = settings.copy()\nautosave_is_configured = \"autosave\" in working\nautosave_value = working.get(\"autosave\", True)\nworking.pop(\"legacy_mode\", None)\nprint(autosave_is_configured, autosave_value)\nprint(settings)",
            [
              "Decide whether absence and a falsey stored value mean different things.",
              "Use a documented default rather than an accidental one.",
              "Snapshot caller data before deletion or replacement.",
            ],
            "Lookup semantics, ownership, and ordering are three separate contracts; make each explicit.",
            "Testing if mapping.get(key) merges a missing key with any present falsey value and can select the wrong branch.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "Choose a collection by the question it answers",
            body: "A dictionary answers 'what value belongs to this identity?' and a set answers 'is this distinct identity present?' The hard work is defining identity: normalization, duplicate meaning, missing values, and deterministic ordering all belong to the data model rather than being cleanup details.",
            steps: [
              { label: "Identify", detail: "Name the fact that uniquely identifies each record or membership item." },
              { label: "Normalize", detail: "Create one canonical representation before hashing, lookup, counting, or comparison." },
              { label: "Aggregate", detail: "Decide whether a repeat replaces, increments, groups, or disappears." },
              { label: "Resolve", detail: "Define missing-key, tie, falsey-value, and output-order policy explicitly." },
              { label: "Protect", detail: "Copy caller-owned mappings or sets before destructive updates unless mutation is promised." },
            ],
          },
          guidedPractice: [
            {
              title: "Count normalized workshop topics",
              prompt: "Plan a frequency table for topic labels that may differ only in case or surrounding whitespace. Keep the example independent from graded tasks.",
              starterCode: "topics = [\" Python \", \"testing\", \"PYTHON\", \" Testing \"]\ncounts = {}\n# normalize each topic before updating counts",
              questions: [
                "Which variations should represent the same key?",
                "What default count is needed for a new key?",
                "Should final presentation preserve first-seen order or use explicit sorting?",
              ],
              reveal: "Define a canonical key first—trimmed and case-normalized—then update one numeric count per key. Choose presentation order separately so the aggregation rule is not entangled with display.",
            },
            {
              title: "Compare two attendance sets",
              prompt: "Given attendees from morning and afternoon sessions, describe how to obtain shared, morning-only, and all distinct participants.",
              starterCode: "morning = {\"Ada\", \"Lin\", \"Grace\"}\nafternoon = {\"Grace\", \"Sam\"}\n# shared = ...\n# morning_only = ...\n# everyone = ...",
              questions: [
                "Which set operation corresponds to each plain-language question?",
                "Why should code not rely on a set's display order?",
                "What information is lost if duplicate visits matter?",
              ],
              reveal: "Translate shared, only, and all-distinct directly into intersection, difference, and union. A set intentionally discards repetition and ordering; add another representation if either matters to the domain.",
            },
          ],
          glossary: [
            { term: "mapping", definition: "A collection associating unique keys with values." },
            { term: "hashable", definition: "Having a stable hash and equality behaviour suitable for dictionary keys or set members." },
            { term: "canonical form", definition: "One normalized representation chosen for values that should compare as equivalent." },
            { term: "membership", definition: "The yes-or-no relationship tested with in." },
            { term: "intersection", definition: "The set of elements present in both operands." },
            { term: "default", definition: "A deliberate fallback used when a requested key is absent." },
          ],
          debugChecklist: [
            "Inspect keys with repr to reveal case and whitespace differences.",
            "Distinguish a missing key from a present key whose stored value is falsey.",
            "Test duplicate inputs according to the chosen replace, count, group, or discard policy.",
            "Use explicit sorting when output order is part of the contract.",
            "Avoid mutating a dictionary while iterating directly over its changing keys.",
            "Snapshot caller mappings and sets before destructive union, update, or pop operations.",
          ],
          checkpoint: {
            question: "Which structure most directly models a collection of unique normalized identifiers with fast membership tests?",
            options: ["A list", "A set", "A tuple", "A formatted string"],
            answerIndex: 1,
            explanation: "A set directly represents distinct membership. Use a dictionary instead when each identifier must carry an associated value.",
          },
        },
        coachConversation: [
          { learner: "Should I choose a dictionary or a set?", coach: "Use a set for a membership question such as is this value present? Use a dictionary when a stable key must retrieve associated information." },
          { learner: "Why does a lookup fail even though the key looks identical?", coach: "Inspect repr(key), then normalize case and surrounding whitespace before storage and lookup. Also distinguish a missing key from a present key whose value is 0, False, or an empty string." },
          { learner: "Can I rely on the collection's order for printed output?", coach: "Do not treat set iteration as sorted. A dictionary preserves insertion order, but the contract may require a different order, so sort explicitly at the output boundary when order is observable." },
        ],
        documentation: [
          { label: "Mapping type: dict", description: "Dictionary construction, lookup, ordering, views, and methods.", url: "https://docs.python.org/3/library/stdtypes.html#mapping-types-dict" },
          { label: "Set types", description: "Distinct membership and union, intersection, difference, and symmetric difference.", url: "https://docs.python.org/3/library/stdtypes.html#set-types-set-frozenset" },
          { label: "Looping techniques", description: "Idiomatic iteration with dictionary items, enumerate, zip, reversed, and sorted.", url: "https://docs.python.org/3/tutorial/datastructures.html#looping-techniques" },
        ],
        runbook: [
          runStep("1. Model the question", "Write what must be retrieved, grouped, or compared and which facts identify it.", "A key → value schema or labelled membership diagram.", "The access question determines whether identity maps to data, represents membership, or needs ordered repetition.", "Write the desired operation as a sentence beginning find by, group by, count by, or is present; its object suggests the key or member."),
          runStep("2. Normalize identifiers", "Canonicalize case, whitespace, and representation before creating keys or set members.", "Equivalent real-world values become equal Python values.", "Hash collections compare Python values exactly, so inconsistent representation creates silent duplicate identities.", "Place normalization in one small function and print repr of raw → canonical pairs for values that should match."),
          runStep("3. Choose duplicate semantics", "Decide whether repeats are preserved, discarded, counted, or grouped.", "A justified choice of list, set, number, or nested record.", "Each collection encodes a different claim about repetition, and accidental deduplication or replacement destroys domain information.", "Create an input where the same identity appears twice with different data, then write the intended final representation before coding."),
          runStep("4. Resolve ambiguity", "Specify missing-key defaults, falsey stored values, ties, and output ordering separately.", "Deterministic results for ambiguous examples.", "Edge policy prevents valid zero or empty values from being mistaken for absence and keeps results reproducible.", "Test missing, present-with-zero, and tied candidates as three different cases; sort only at the final presentation boundary if required."),
          runStep("5. Protect ownership", "Copy before destructive updates unless mutation is part of the contract.", "Caller snapshots and ordered output evidence.", "Dictionary and set update operations mutate shared objects, which can make later callers observe surprising state.", "Check object identity, make a copy of the container, and compare the source snapshot after the transformation completes."),
        ],
      },

      py08: {
        title: "Recursion",
        subtitle: "Solve a problem by reducing it to smaller instances with a direct stopping answer.",
        objectives: [
          "Define base cases and prove recursive progress.",
          "Trace values while the call stack unwinds.",
          "Recognize structural recursion and branching search.",
        ],
        tutorial: [
          section(
            "Pair a base case with a progress measure",
            "Recursion solves a problem by asking the same function to solve a strictly smaller version. The base case answers the smallest valid version directly. Before coding, name a measure—such as exponent, remaining length, or tree depth—that decreases on every recursive path and cannot decrease forever. This is the termination proof.",
            "def integer_power(base, exponent):\n    if exponent == 0:\n        return 1\n    return base * integer_power(base, exponent - 1)\n\nprint(integer_power(3, 4))",
            [
              "Write the direct answer for the smallest valid problem.",
              "Name the non-negative measure that strictly decreases.",
              "Check every branch, not only the common one, for progress.",
            ],
            "Mental model: each call holds one unfinished multiplication while delegating a smaller exponent to the next stack frame.",
            "Allowing a negative exponent without a matching rule makes exponent - 1 move away from the base forever.",
          ),
          section(
            "Trace descent and unwind separately",
            "During descent, each call records its local values and waits for a smaller call. When the base returns, suspended expressions resume in reverse order. A useful trace has two columns: calls going down and substitutions coming back up. This reveals whether work belongs before the recursive call, after it, or in both places.",
            "def triangular_total(number):\n    if number == 0:\n        return 0\n    smaller_total = triangular_total(number - 1)\n    return number + smaller_total\n\n# descent: 4, 3, 2, 1, 0\n# unwind:  0, 1, 3, 6, 10",
            [
              "Expand calls until the first direct return.",
              "Record one local frame per call rather than overwriting values mentally.",
              "Substitute return values upward in reverse call order.",
            ],
            "Recursive evaluation has two stories: reducing the problem and combining the returned answers.",
            "Printing only during descent can make it look as if nothing happens during unwind, where the result may actually be assembled.",
          ),
          section(
            "Let recursive code mirror recursive data",
            "Tree-shaped data contains smaller values of the same overall kind. Define every node shape: here a file is a leaf with a direct size, while a directory contains child nodes whose sizes must be combined. Return the same type from every shape so the parent can combine child results without special cases.",
            "def storage_used(node):\n    if node[\"kind\"] == \"file\":\n        return node[\"bytes\"]\n    total = 0\n    for child in node[\"children\"]:\n        total += storage_used(child)\n    return total\n\nproject = {\"kind\": \"folder\", \"children\": [\n    {\"kind\": \"file\", \"bytes\": 120},\n    {\"kind\": \"file\", \"bytes\": 80},\n]}\nprint(storage_used(project))",
            [
              "List every legal node shape and its direct data.",
              "Return one consistent type from leaves and containers.",
              "Test an empty container, one leaf, and several nesting levels.",
            ],
            "Mental model: the data definition is the blueprint for the recursive branches.",
            "Failing to distinguish a leaf from a container before reading children causes missing-key or type errors at the deepest level.",
          ),
          section(
            "Cache repeated states in a branching graph",
            "Branching recursion explores several smaller choices. Different branches may arrive at the same state, causing repeated work. Define the state with immutable values, cache its completed answer, and ensure the dependency graph has no cycles. Also estimate maximum stack depth separately from total call count; caching reduces repeated calls but does not automatically reduce depth.",
            "from functools import lru_cache\n\ndependencies = {\n    \"web\": (\"api\", \"ui\"),\n    \"api\": (\"database\",),\n    \"ui\": (\"theme\",),\n}\n\n@lru_cache(maxsize=None)\ndef dependency_depth(package):\n    children = dependencies.get(package, ())\n    if not children:\n        return 1\n    return 1 + max(dependency_depth(child) for child in children)\n\nprint(dependency_depth(\"web\"))",
            [
              "Describe the entire state using immutable cacheable arguments.",
              "Identify where different branches can revisit that same state.",
              "Detect or forbid cycles and estimate maximum depth.",
            ],
            "Memoization changes repeated exploration into one computation per distinct state.",
            "Caching a function that depends on hidden mutable global state can return stale answers even when its arguments match.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "Trust a smaller version under a strict progress rule",
            body: "Recursive reasoning separates two obligations. Direct cases answer the smallest valid inputs without another call. Recursive cases transform the input into strictly smaller legal subproblems, trust their promised results, and combine those results. The call stack remembers unfinished combinations until the base answers begin to return.",
            steps: [
              { label: "Measure", detail: "Choose a non-negative size, depth, or distance that describes how far the input is from a direct case." },
              { label: "Anchor", detail: "Return a correctly typed answer for every smallest or empty shape without making another call." },
              { label: "Reduce", detail: "Make each recursive argument legal and strictly smaller by the chosen measure." },
              { label: "Combine", detail: "Build the current answer from the smaller call's documented return rather than shared global state." },
              { label: "Budget", detail: "Count depth and repeated subproblems to decide whether memoization or iteration is safer." },
            ],
          },
          guidedPractice: [
            {
              title: "Trace nested gift boxes",
              prompt: "A box either contains a note or one smaller box. Design the base case and trace calls needed to find the note; do not write a course exercise solution.",
              starterCode: "def find_note(box):\n    # What shape is directly answerable?\n    # How is the smaller contained box selected?\n    pass",
              questions: [
                "What structural measure strictly decreases?",
                "What type must both base and recursive paths return?",
                "Which malformed shape could prevent progress?",
              ],
              reveal: "Depth remaining is the progress measure. A note-bearing box returns directly; a wrapper delegates only to its contained smaller box. Both paths must satisfy one return contract, and malformed cycles need rejection rather than recursion.",
            },
            {
              title: "Unwind a short text count",
              prompt: "Plan a recursive character count for a short text using a first-character/rest decomposition, then draw the stack for three characters.",
              starterCode: "def character_count(text):\n    # Base: which text needs no smaller call?\n    # Recursive case: first item plus result for a suffix\n    pass",
              questions: [
                "What value should empty text contribute?",
                "How does slicing prove progress?",
                "In what order do pending additions complete during unwind?",
              ],
              reveal: "Empty text provides the neutral count. Each non-empty call removes one character, so length strictly decreases. The stack stores one pending contribution per character and completes them from the deepest call outward.",
            },
          ],
          glossary: [
            { term: "base case", definition: "A directly answerable input that returns without another recursive call." },
            { term: "recursive case", definition: "A path that delegates to one or more smaller legal instances." },
            { term: "call stack", definition: "The runtime structure holding active calls and their unfinished local work." },
            { term: "recurrence", definition: "A rule defining an answer in terms of answers to smaller instances." },
            { term: "memoization", definition: "Caching results for previously solved states to avoid repeated recursion." },
            { term: "stack depth", definition: "The maximum number of simultaneously active calls along one path." },
          ],
          debugChecklist: [
            "Call each base case directly and verify its return type before testing recursion.",
            "Write the progress measure beside every recursive argument and prove it becomes smaller.",
            "Trace active frames with argument, pending operation, and returned value columns.",
            "Check that every branch either returns directly or returns a composed recursive result.",
            "Look for repeated states that suggest memoization or a different algorithm.",
            "Test malformed cyclic or non-shrinking data when the input domain could contain it.",
          ],
          checkpoint: {
            question: "What is the essential termination argument for a recursive function?",
            options: ["It contains a return statement somewhere", "Every recursive path reaches a direct case because a bounded measure strictly shrinks", "Python stops calls after a convenient number", "The function calls itself only once per line"],
            answerIndex: 1,
            explanation: "A base case is useful only when every recursive path is guaranteed to approach it through strictly smaller valid states.",
          },
        },
        coachConversation: [
          { learner: "I wrote a base case. Why can the recursion still continue forever?", coach: "A base case only helps if every recursive path reaches it. Name a bounded progress measure—such as remaining length—and prove that each recursive argument makes it strictly smaller." },
          { learner: "I understand the calls going down, but not the final result.", coach: "Trace two directions separately. Record arguments and pending work during descent, then substitute each returned value while the call stack unwinds." },
          { learner: "When should I cache recursive results?", coach: "First identify repeated states with the same inputs. functools.cache can remove repeated computation for suitable pure calls, but it cannot repair a missing base case or a non-shrinking argument. A RecursionError is evidence to inspect progress and depth first; raising the recursion limit is rarely the real algorithmic fix." },
        ],
        documentation: [
          { label: "Defining functions", description: "Function calls, parameters, scope, and return behaviour used by recursive definitions.", url: "https://docs.python.org/3/tutorial/controlflow.html#defining-functions" },
          { label: "functools.cache", description: "A lightweight memoization decorator for repeated calls with hashable arguments.", url: "https://docs.python.org/3/library/functools.html#functools.cache" },
          { label: "Recursion limit", description: "How to inspect Python's interpreter recursion-depth safeguard.", url: "https://docs.python.org/3/library/sys.html#sys.getrecursionlimit" },
        ],
        runbook: [
          runStep("1. Define direct cases", "List every smallest number, empty shape, or leaf that can be answered without another call.", "Base cases with types and expected returns.", "Base cases establish both termination anchors and the values from which larger answers are composed.", "Call the function only with the smallest valid shapes first; if their outputs are ambiguous, the recursive rule is not ready."),
          runStep("2. Prove progress", "Name a bounded measure that strictly shrinks on every recursive path.", "A sentence explaining why the base must be reached.", "Syntactic self-calls do not imply progress; a well-founded decreasing measure supplies the actual termination proof.", "Write measure(current) and measure(next) for every branch; any branch without a strict inequality is the likely infinite path."),
          runStep("3. Express smaller work", "Build the current answer only from legal, smaller subproblems with consistent result types.", "A recurrence or data-shape rule in plain language.", "Consistent contracts let each frame treat the smaller result as trustworthy without inspecting its implementation.", "Pretend the smaller call already works and name exactly what it returns; then write only the combination step around that value."),
          runStep("4. Trace both directions", "Record stack frames during descent and substitute values during unwind.", "An annotated call tree with downward and upward arrows.", "Descent explains problem reduction, while unwind explains result construction; seeing only one direction hides half the algorithm.", "Use a three-unit input and create one row per active frame with argument and pending operation, then fill return values bottom-up."),
          runStep("5. Budget resources", "Estimate distinct states, repeated states, total calls, and maximum depth.", "A justified choice among plain recursion, memoization, and iteration.", "A correct recurrence can still repeat exponentially or exceed the interpreter's practical stack depth.", "Draw the first few levels of the call tree; merge equal states mentally and compare total nodes with distinct states."),
        ],
      },

      py09: {
        title: "FP with Collections",
        subtitle: "Describe collection work as composable selection, transformation, reduction, and lazy production.",
        objectives: [
          "Design clear filter → map → reduce pipelines.",
          "Write focused, pure callback functions.",
          "Control when lazy iterators are consumed.",
        ],
        tutorial: [
          section(
            "Sketch data stages before choosing syntax",
            "A collection pipeline describes movement from a source through focused stages: select items, transform each survivor, and optionally combine or materialize the results. Write the value type at every arrow and explain one rejected and one accepted item. Only then choose filter, map, a comprehension, reduce, or a generator for each stage.",
            "filenames = [\"app.py\", \"notes.txt\", \"tests.py\", \"README.md\"]\n# strings -> Python filenames -> display labels -> list\npython_files = filter(lambda name: name.endswith(\".py\"), filenames)\nlabels = map(lambda name: name.removesuffix(\".py\").upper(), python_files)\nresult = list(labels)\nprint(result)",
            [
              "Annotate source, intermediate, and final types.",
              "Give each stage one verb and one responsibility.",
              "Trace one value that survives and one that is rejected.",
            ],
            "Mental model: a pipeline is an assembly line; each station receives one kind of value and emits another.",
            "Reordering stages can change meaning, raise errors for values that should have been rejected, or do unnecessary work.",
          ),
          section(
            "Keep predicates and transformations single-purpose",
            "A filter predicate receives one item and returns a truth value answering whether it survives. A map callback receives one item and returns its replacement. Small named functions are often clearer than long lambdas, especially when normalization has several steps. Keep callbacks free of external mutation so the same input always produces the same stage behaviour.",
            "accounts = [\"  ADA \" , \"\", \"  lin\", \"grace  \"]\n\ndef has_name(raw_name):\n    return bool(raw_name.strip())\n\ndef normalize_name(raw_name):\n    return raw_name.strip().casefold()\n\nactive_names = filter(has_name, accounts)\nnormalized_names = map(normalize_name, active_names)\nprint(list(normalized_names))",
            [
              "Make the predicate answer one yes/no question.",
              "Make the mapper produce one replacement value.",
              "Test callbacks independently before composing them.",
            ],
            "One-item callbacks are reusable, testable vocabulary for a larger pipeline.",
            "A callback that appends elsewhere and returns nothing causes map to yield None while hiding the actual result in external state.",
          ),
          section(
            "Fold only when the accumulator has a clear meaning",
            "reduce combines a stream into one accumulator. State what the accumulator means after processing the first k items and choose an initializer that also defines empty-input behaviour. Prefer a purpose-built operation such as sum, min, max, any, all, or join when it expresses the same intention more directly.",
            "from functools import reduce\n\ndef choose_longer(current, candidate):\n    if len(candidate) > len(current):\n        return candidate\n    return current\n\nlabels = [\"ok\", \"processing\", \"done\"]\nlongest_label = reduce(choose_longer, labels, \"\")\nprint(longest_label)",
            [
              "Write the accumulator invariant in one sentence.",
              "Define what empty input should return through the initializer.",
              "Use a named reducer when tie or branch rules need explanation.",
            ],
            "Mental model: each item and the answer-so-far become the next answer-so-far.",
            "An initializer with the wrong type can make the reducer's first call inconsistent with all later calls.",
          ),
          section(
            "Generators let consumers pull values on demand",
            "Calling a generator function creates an iterator without executing its body. Each next request resumes execution until yield, returns that value, and pauses while preserving local state. This enables early stopping and bounded memory, but the iterator is normally single-use. Keep file handles or mutable sources in mind because consumption may happen later than creation.",
            "def nonblank_lines(lines):\n    for line in lines:\n        cleaned = line.strip()\n        if cleaned:\n            yield cleaned\n\nstream = nonblank_lines([\" alpha \", \"   \", \"beta\"] )\nprint(next(stream))\nprint(list(stream))",
            [
              "Define one logical value per yield.",
              "Test creation, one next call, and remaining consumption separately.",
              "Create a fresh iterator when a second traversal is required.",
            ],
            "Mental model: a generator is a paused producer whose consumer controls the clock.",
            "Converting a generator to a list for inspection exhausts it; later iteration sees no values.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "A typed pipeline of one-purpose stages",
            body: "Functional collection work is easiest to reason about as arrows between value types. A predicate selects, a mapper replaces, a reducer summarizes, and a generator schedules production. Each stage should have one semantic verb, receive the type promised by the prior stage, and avoid changing unrelated state.",
            steps: [
              { label: "Source", detail: "Name the iterable, its item type, and whether it is reusable or one-shot." },
              { label: "Select", detail: "Use a predicate that answers exactly one membership question for one item." },
              { label: "Transform", detail: "Map each surviving item to one replacement value with a clearly stated type." },
              { label: "Combine", detail: "Materialize, reduce, or join only when the consumer needs a final aggregate." },
              { label: "Consume", detail: "Make laziness and iterator ownership explicit, especially after partial traversal." },
            ],
          },
          guidedPractice: [
            {
              title: "Plan a sensor-reading pipeline",
              prompt: "From a mixture of numeric readings and missing values, plan stages that keep real numbers, convert them to descriptive records, and materialize only at the boundary.",
              starterCode: "readings = [12.5, None, 18.0, -2.0, None]\n# valid = filter(...)\n# records = map(...)\n# result = ...",
              questions: [
                "What exact yes/no question should the predicate answer?",
                "What type enters and leaves the transformation stage?",
                "At what point does a concrete list become necessary?",
              ],
              reveal: "Annotate the arrows first: optional numbers → numbers → records → list. Keep absence filtering separate from record construction, and delay list creation until a consumer actually needs repeatable concrete data.",
            },
            {
              title: "Design a bounded code generator",
              prompt: "Sketch a generator that yields labels for a supplied finite sequence of identifiers. Focus on lifecycle rather than completing any graded function.",
              starterCode: "def labelled_ids(identifiers):\n    for identifier in identifiers:\n        # yield one transformed value\n        pass\n\nstream = labelled_ids([3, 8, 13])",
              questions: [
                "When does the generator body first execute?",
                "What local state survives between next() calls?",
                "What will remain after one value is consumed and the rest is converted to a list?",
              ],
              reveal: "Calling the function creates a paused iterator; the first next request starts execution. Each yield returns one value and preserves the loop position. Later materialization consumes only the remaining items, not the already yielded one.",
            },
          ],
          glossary: [
            { term: "higher-order function", definition: "A function that receives or returns another function." },
            { term: "predicate", definition: "A one-item callback returning a truth value for selection." },
            { term: "mapper", definition: "A one-item callback producing the item's replacement value." },
            { term: "reducer", definition: "A callback combining an accumulator and one item into the next accumulator." },
            { term: "lazy evaluation", definition: "Producing values only when a consumer requests them." },
            { term: "materialization", definition: "Consuming an iterable to build a concrete collection such as a list." },
          ],
          debugChecklist: [
            "Write the item type before and after every pipeline stage.",
            "Test predicates and mappers independently with one accepted and one rejected value.",
            "Check callback return values for accidental None caused by mutation-only functions.",
            "Give reducers an initializer that defines empty-input behaviour and matches the accumulator type.",
            "Track whether an iterator has been partially or fully consumed before attempting reuse.",
            "Materialize a tiny diagnostic stream once, then recreate it before the actual pipeline run.",
          ],
          checkpoint: {
            question: "What happens when a generator function is called?",
            options: ["Its body runs to completion and returns a list", "It creates an iterator; execution begins when values are requested", "It yields its first value immediately and discards local state", "It can be traversed repeatedly without recreation"],
            answerIndex: 1,
            explanation: "A generator call creates a lazy iterator. Execution advances to each yield on demand and normally supports one traversal.",
          },
        },
        coachConversation: [
          { learner: "Should I start with map, filter, or a comprehension?", coach: "Start with the data story: source → select → transform → combine. Choose syntax only after each arrow has a clear input and output type; a comprehension is often easiest to read when the pipeline is short." },
          { learner: "Why did my callback produce None values?", coach: "A mutating method such as list.append returns None. Keep predicates and transformations focused on returning a result, and test each callback with one tiny value before composing the pipeline." },
          { learner: "Where did my generator's values go on the second pass?", coach: "Iterators are usually one-use and lazy. For example, map(int, input().split()) converts tokens only as they are consumed. Recreate the iterator after consumption, or materialize it with list(...) when you intentionally need indexing or reuse and can afford the memory." },
        ],
        documentation: [
          { label: "Functional Programming HOWTO", description: "Iterators, generators, built-ins, and composition from Python's official HOWTO.", url: "https://docs.python.org/3/howto/functional.html" },
          { label: "Built-in map()", description: "Lazy application of a function across one or more iterables.", url: "https://docs.python.org/3/library/functions.html#map" },
          { label: "Built-in filter()", description: "Lazy selection of iterable values through a predicate.", url: "https://docs.python.org/3/library/functions.html#filter" },
          { label: "functools.reduce", description: "Cumulative reduction and the role of an initializer.", url: "https://docs.python.org/3/library/functools.html#functools.reduce" },
        ],
        runbook: [
          runStep("1. Diagram stages", "Write source → select → transform → combine or yield, with a type at each arrow.", "A pipeline sketch plus one traced source item.", "Type-labelled stages reveal semantic mismatches and make changes in order reviewable before compact syntax hides them.", "Expand the pipeline into named intermediate variables and annotate a single item's value and type after each arrow."),
          runStep("2. Isolate callbacks", "Give predicates, mappers, and reducers one responsibility and test them separately.", "Tiny assertions for accepted, rejected, and transformed items.", "Small pure callbacks turn an opaque pipeline into independently verifiable vocabulary.", "Replace the lambda with a named function, call it directly, and inspect both returned value and type for one edge item."),
          runStep("3. Compose semantically", "Order stages from domain meaning, then verify that rejected values never reach unsafe transformations.", "A small end-to-end trace through every stage.", "Stage order affects correctness, exceptions, and work performed; it is part of the algorithm rather than style.", "Trace one survivor and one rejected item; if the rejected item reaches a transformation, move or refine selection."),
          runStep("4. Choose evaluation time", "Decide whether callers need a concrete collection now or a one-use iterator later.", "An explicit and justified materialization point.", "Laziness saves memory and supports early stopping, while concrete collections support repeated access and stable snapshots.", "Ask whether the consumer needs length, indexing, multiple passes, or only one forward pass; materialize only for the former needs."),
          runStep("5. Probe lifecycle", "Test empty input, all-rejected input, duplicates, partial consumption, and attempted reuse.", "Expected values plus iterator state at each step.", "Iterator state is invisible in its type, so lifecycle tests prevent debugging inspections from accidentally consuming real work.", "Create a fresh iterator, call next once, record that value, then list the remainder and verify a second traversal is empty by design."),
        ],
      },

      py10: {
        title: "Effect-Free Programming",
        subtitle: "Build transformations that are predictable because inputs and external state remain untouched.",
        objectives: [
          "Recognize and isolate side effects.",
          "Create new values with comprehensions and generators.",
          "Use closures without hidden mutable surprises.",
        ],
        tutorial: [
          section(
            "Draw a boundary around effects",
            "Input, print, files, time, randomness, network calls, and mutation all connect code to changing state. A pure function depends only on its arguments, returns a value, and leaves caller-owned objects untouched. Keep effects in a thin outer layer and pass ordinary values into a deterministic core. This makes repeated calls and tests independent of the environment.",
            "def translate_points(points, horizontal, vertical):\n    return [\n        (x + horizontal, y + vertical)\n        for x, y in points\n    ]\n\nsource = [(1, 2), (4, -1)]\nmoved = translate_points(source, 3, 5)\nassert source == [(1, 2), (4, -1)]\nprint(moved)",
            [
              "Inventory every external read, write, and mutation.",
              "Define ownership of mutable arguments before implementation.",
              "Pass changing environmental data in as explicit values.",
            ],
            "Mental model: a pure core is a value transformer; the shell handles the changing world.",
            "Returning a new outer list is not enough if the function still mutates nested objects shared with the caller.",
          ),
          section(
            "Use comprehensions as readable value builders",
            "A comprehension declares the output expression, source, and optional selection rule. Read it aloud as 'produce this for each item in that source when this condition holds.' It should create a new collection without side effects. If the expression or condition contains several domain rules, move them into a named helper or use a conventional loop.",
            "raw_topics = [\"  Python\", \"testing  \", \"\", \"APIs\"]\nclean_topics = [\n    topic.strip().casefold()\n    for topic in raw_topics\n    if topic.strip()\n]\nlength_by_topic = {topic: len(topic) for topic in clean_topics}\nprint(clean_topics)\nprint(length_by_topic)",
            [
              "Read expression, loop, and filter clauses in that order.",
              "Keep mutation and print out of the expression.",
              "Split separate conceptual stages into named comprehensions.",
            ],
            "Mental model: a comprehension describes the membership and value of a newly constructed collection.",
            "Repeating an expensive cleanup expression in both the result and filter clauses performs the work twice per item.",
          ),
          section(
            "Keep lazy production deterministic",
            "A generator can remain effect-light when its output depends only on the iterable passed to it. Local state persists between yields, so document what it summarizes. Avoid reading the clock, random state, or a globally changing collection during consumption; otherwise creating the generator twice with equal arguments may produce different streams.",
            "def running_totals(values):\n    total = 0\n    for value in values:\n        total += value\n        yield total\n\nfirst_pass = list(running_totals([4, -1, 6]))\nsecond_pass = list(running_totals([4, -1, 6]))\nassert first_pass == second_pass\nprint(first_pass)",
            [
              "State the meaning of local state after each yield.",
              "Treat the supplied source as read-only unless documented otherwise.",
              "Compare complete outputs from two fresh equal calls.",
            ],
            "Laziness changes when values are produced, not the requirement that equal inputs behave predictably.",
            "Reusing the same exhausted generator is not a repeatability test; create a fresh generator for each run.",
          ),
          section(
            "Use closures for stable configuration",
            "A closure is an inner function that retains names from the enclosing call after that call has returned. Capturing immutable configuration creates a predictable specialized function. Capture a normalized value rather than a caller-owned mutable object, and avoid counters or histories unless statefulness is an explicit requirement.",
            "def make_prefix_checker(prefix):\n    normalized_prefix = prefix.casefold()\n\n    def starts_with_prefix(text):\n        return text.casefold().startswith(normalized_prefix)\n\n    return starts_with_prefix\n\nis_error = make_prefix_checker(\"ERROR:\")\nassert is_error(\"error: disk full\") is True\nassert is_error(\"warning: disk slow\") is False",
            [
              "List every captured name and whether it can change.",
              "Normalize and freeze configuration at factory time.",
              "Call the returned function twice with equal input and compare results.",
            ],
            "Mental model: a closure packages a rule together with a small, stable environment.",
            "Capturing a mutable configuration dictionary means later caller edits can silently change the returned function's behaviour.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "A pure core inside an effectful shell",
            body: "Programs must eventually interact with people, files, clocks, or networks, but those effects do not need to permeate every calculation. A thin shell acquires changing facts and publishes results; a pure core transforms explicit values without mutation. Equal arguments can then be reasoned about, repeated, and tested independently of execution history.",
            steps: [
              { label: "Inventory", detail: "Mark every read or write involving input, output, globals, mutation, time, randomness, files, or networks." },
              { label: "Inject", detail: "Pass environmental facts and configuration into the core as ordinary explicit arguments." },
              { label: "Transform", detail: "Construct new values from arguments while leaving caller-owned data untouched." },
              { label: "Capture", detail: "When using a closure, retain normalized immutable configuration rather than hidden mutable history." },
              { label: "Repeat", detail: "Compare fresh equal calls and source snapshots to prove independence from order and prior use." },
            ],
          },
          guidedPractice: [
            {
              title: "Extract a pure price transformation",
              prompt: "A shell already reads a discount rate and a list of prices. Design only a pure core that produces discounted values without changing its inputs.",
              starterCode: "def apply_discount(prices, rate):\n    # Return a new collection; do not print or mutate prices.\n    pass\n\noriginal = [8.0, 12.5, 20.0]",
              questions: [
                "Which inputs contain everything needed for a deterministic result?",
                "How will a test prove that original remains unchanged?",
                "Where should display formatting happen?",
              ],
              reveal: "The core needs only prices and rate and should construct a new collection. A test snapshots the original, compares the returned values, and confirms the source still matches its snapshot; the outer shell handles formatting.",
            },
            {
              title: "Configure a stable suffix rule",
              prompt: "Plan a closure factory that captures a text suffix once and returns a checker. Focus on captured-state reasoning rather than the finished implementation.",
              starterCode: "def make_suffix_checker(suffix):\n    normalized_suffix = ...\n\n    def has_suffix(text):\n        # depend only on text and normalized_suffix\n        pass\n\n    return has_suffix",
              questions: [
                "Which value should be normalized at factory time?",
                "Can the captured value change after the factory returns?",
                "How can two equal calls demonstrate repeatability?",
              ],
              reveal: "Capture one normalized immutable string, not a caller-owned mutable configuration. The returned function then depends only on its text argument and that stable captured value, so equal calls give equal results.",
            },
          ],
          glossary: [
            { term: "pure function", definition: "A function whose result depends only on arguments and which causes no externally observable change." },
            { term: "referential transparency", definition: "The property that an expression can be replaced by its value without changing program behaviour." },
            { term: "effect", definition: "Interaction with changing state beyond returning a value." },
            { term: "deterministic", definition: "Producing the same observable result for the same explicit inputs." },
            { term: "closure", definition: "A function retaining bindings from the enclosing lexical scope after that scope returns." },
            { term: "defensive copy", definition: "A copy made to prevent external mutation from affecting owned state or vice versa." },
          ],
          debugChecklist: [
            "Search the core for input, print, files, time, randomness, network access, global reads, and mutation.",
            "Snapshot every mutable argument before the call and compare it afterward.",
            "Run the core twice with independently created equal inputs and compare returned values.",
            "Inspect nested objects when a shallowly new outer collection may still share mutable children.",
            "List closure-captured names and classify each as immutable configuration or changing state.",
            "Create fresh generators for repeatability checks rather than reusing an exhausted iterator.",
          ],
          checkpoint: {
            question: "Which design best supports an effect-free calculation core?",
            options: ["Read the current time inside every helper", "Pass required facts as arguments and return newly constructed values", "Store intermediate results in a global list", "Print each partial result instead of returning it"],
            answerIndex: 1,
            explanation: "Explicit arguments expose dependencies, and new return values avoid hidden mutation. Effects can remain in a thin outer shell.",
          },
        },
        coachConversation: [
          { learner: "Does pure code mean my program cannot print or read input?", coach: "The whole program still needs effects. Keep them in a thin shell: raw_text = input(); count = int(raw_text); result = pure_core(count); print(result). The core receives an ordinary value, returns a new value, and stays easy to test." },
          { learner: "Why does the same function call produce a different result later?", coach: "Inventory hidden dependencies: globals, current time, randomness, shared mutable objects, and consumed iterators. Make required facts explicit parameters and avoid changing caller-owned data." },
          { learner: "Can a closure remember configuration safely?", coach: "Yes—capture normalized immutable configuration when the closure is created. If captured state changes between calls, document that effect instead of presenting the function as repeatable." },
        ],
        documentation: [
          { label: "List comprehensions", description: "Constructing new values declaratively without mutating the source list.", url: "https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions" },
          { label: "Generators", description: "Lazy functions that preserve local state between yielded values.", url: "https://docs.python.org/3/tutorial/classes.html#generators" },
          { label: "Binding of names", description: "How local, nonlocal, and global names are resolved in Python execution scopes.", url: "https://docs.python.org/3/reference/executionmodel.html#binding-of-names" },
          { label: "itertools", description: "Composable iterator building blocks for efficient lazy pipelines.", url: "https://docs.python.org/3/library/itertools.html" },
        ],
        runbook: [
          runStep("1. Inventory effects", "Mark input, output, files, time, randomness, network use, globals, and mutation.", "A boundary-versus-pure-core diagram.", "You can isolate only the effects you have identified, and overlooked reads are hidden dependencies just like writes.", "Highlight verbs that touch the outside world, then draw a boundary around the smallest shell that can own them."),
          runStep("2. Make dependencies explicit", "Pass environmental facts and configuration through parameters instead of reading hidden state.", "A core whose result is determined by its arguments.", "Explicit dependencies make equal-call reasoning and focused tests possible without arranging global environment state.", "For each global or environmental read, add a parameter representing the needed fact rather than passing a service when a value is enough."),
          runStep("3. Construct new values", "Use clear comprehensions, tuples, or yields without modifying caller-owned objects.", "Result data plus unchanged input snapshots.", "New values preserve caller ownership and remove history-dependent behaviour caused by shared mutation.", "Snapshot input, build the simplest result container from scratch, and compare both result and snapshot after one call."),
          runStep("4. Freeze configuration", "Let closures capture normalized immutable values and document any deliberate state.", "Independent configured functions with named captured values.", "Stable captures produce specialized rules; mutable captures turn call order and outside edits into hidden inputs.", "List every free variable used by the inner function and replace mutable configuration with the exact immutable fields it needs."),
          runStep("5. Prove repeatability", "Run fresh calls with equal inputs and compare values, streams, and source snapshots.", "Evidence that order, history, and environment do not alter the core.", "Repeatability is behavioural evidence that dependencies are explicit and mutation has not leaked across calls.", "Create two independently allocated but equal inputs, call in different orders, and compare results plus post-call inputs."),
        ],
      },

      py11: {
        title: "Divide and Conquer",
        subtitle: "Use structural promises and invariants to discard large parts of a problem safely.",
        objectives: [
          "Define binary-search boundaries without off-by-one errors.",
          "Exploit monotone and bitonic structure.",
          "Connect algorithm choice to time complexity.",
        ],
        tutorial: [
          section(
            "Divide a problem without losing the answer",
            "Divide and conquer splits a problem into smaller independent regions, solves those regions, and combines their answers. The invariant states what remains true about the current region. For finding a maximum, the maximum of the whole sequence must be the larger maximum of its two non-empty halves, so neither half is discarded without contributing evidence.",
            "def maximum_between(values, start, stop):\n    # Region uses half-open bounds: start <= index < stop.\n    if stop - start == 1:\n        return values[start]\n    middle = (start + stop) // 2\n    left_maximum = maximum_between(values, start, middle)\n    right_maximum = maximum_between(values, middle, stop)\n    return max(left_maximum, right_maximum)\n\nscores = [18, 41, 7, 29, 36]\nprint(maximum_between(scores, 0, len(scores)))",
            [
              "State what the current region contains and excludes.",
              "Stop at a region whose answer is direct.",
              "Explain how subanswers combine into the parent answer.",
            ],
            "Mental model: every recursive frame owns one region, delegates smaller regions, and combines their evidence.",
            "Splitting an empty region indefinitely means the base case and initial domain do not agree; validate non-empty input or define an empty result.",
          ),
          section(
            "Choose one interval convention and keep it",
            "An inclusive region [start, end] contains both boundaries; a half-open region [start, stop) includes start and excludes stop. Half-open regions make their size stop - start and allow an empty region when both bounds match. Whichever convention you choose must determine initialization, base cases, slice boundaries, and updates consistently.",
            "def split_region(start, stop):\n    # Half-open region [start, stop).\n    middle = start + (stop - start) // 2\n    left = (start, middle)\n    right = (middle, stop)\n    return left, right\n\nprint(split_region(3, 10))   # sizes 3 and 4\nprint(split_region(5, 5))    # two empty regions",
            [
              "Write the convention beside the bounds.",
              "Calculate region size from that convention.",
              "Test empty, one-element, two-element, odd, and even regions.",
            ],
            "Mental model: bounds define a mathematical set of candidate indexes, not vague pointers near the answer.",
            "Mixing an inclusive initial end with a half-open stop condition can skip the final element or index beyond the list.",
          ),
          section(
            "Search a monotone predicate, not arbitrary data",
            "Halving is safe when the observations have a one-way structure. A monotone predicate is false for an initial region and true from some boundary onward, or the reverse. The goal is to find that boundary while preserving a candidate interval. First prove that the predicate cannot switch back; without that promise, one middle observation does not justify discarding a side.",
            "# Find the smallest batch whose measured duration reaches a limit.\ndef first_slow_batch(durations, limit):\n    start, stop = 0, len(durations)\n    while start < stop:\n        middle = (start + stop) // 2\n        if durations[middle] < limit:\n            start = middle + 1\n        else:\n            stop = middle\n    return start\n\nmeasurements = [12, 18, 27, 35, 49, 63]\nprint(first_slow_batch(measurements, 40))",
            [
              "State and verify the one-way structural promise.",
              "Define the answer when every item is false or every item is true.",
              "Explain whether the middle index remains a possible boundary in each branch.",
            ],
            "Mental model: a monotone predicate turns many values into a single false-to-true frontier.",
            "Stopping at the first true midpoint finds a true item, not necessarily the earliest true item; retain it as a candidate and continue left.",
          ),
          section(
            "Account for divide, solve, and combine costs",
            "Complexity depends on the number of subproblems, their size, and the work used to combine answers. Repeated halving along one path gives logarithmic depth. Visiting both halves can still touch every element. Copying slices adds work and memory, so passing index bounds often preserves the intended complexity more faithfully.",
            "competitors = 1_024\nrounds = 0\nremaining = competitors\nwhile remaining > 1:\n    remaining = (remaining + 1) // 2\n    rounds += 1\nprint(rounds)  # ten rounds of halving\n\n# A tournament has logarithmic rounds but still many total matches.",
            [
              "Name n and describe subproblem sizes after one level.",
              "Separate maximum depth from total work across all branches.",
              "Include slicing, combining, and helper costs in the budget.",
            ],
            "Mental model: depth measures the longest dependency chain; total work measures every node of the computation tree.",
            "Calling an O(n) helper at each supposedly logarithmic search step can turn a fast-looking design into linear or worse work.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "Discard only what the invariant proves irrelevant",
            body: "Divide-and-conquer algorithms earn speed by reducing a candidate region. The input's structural promise—sortedness, monotonicity, or independent halves—turns a middle observation into evidence. Bounds describe exactly which candidates remain, and every update must preserve that claim while making the region strictly smaller.",
            steps: [
              { label: "Promise", detail: "State the sorted, monotone, bitonic, or recursively composable property and give a counterexample without it." },
              { label: "Region", detail: "Choose inclusive or half-open bounds and write the exact candidate set they represent." },
              { label: "Observe", detail: "Evaluate a midpoint or smaller subanswer and state precisely what it proves." },
              { label: "Reduce", detail: "Discard or combine regions only according to that proof, preserving every possible answer." },
              { label: "Account", detail: "Count recursive branches, longest depth, combination work, copying, and auxiliary storage." },
            ],
          },
          guidedPractice: [
            {
              title: "Locate a schedule threshold",
              prompt: "A sorted list of cumulative minutes becomes late at some boundary. Design the invariant and interval updates that would locate the first late value without completing a graded search function.",
              starterCode: "minutes = [8, 14, 23, 31, 45, 58]\nlimit = 30\nstart, stop = 0, len(minutes)\n# invariant: the first qualifying index remains in ...\n# while the region is non-empty: ...",
              questions: [
                "Why is the predicate monotone for sorted minutes?",
                "Does a qualifying midpoint remain a possible first qualifying index?",
                "What index represents the case where no value qualifies?",
              ],
              reveal: "Think in terms of a false-to-true boundary over a half-open candidate region. A non-qualifying midpoint proves it and everything left can be discarded; a qualifying midpoint remains a candidate, so retain its side while searching earlier positions.",
            },
            {
              title: "Audit interval conventions",
              prompt: "For regions of lengths zero through four, write start, stop, size, midpoint, and child bounds using one half-open convention.",
              starterCode: "def describe_region(start, stop):\n    size = stop - start\n    # middle = ...\n    # left and right bounds = ...\n    pass",
              questions: [
                "How does the convention represent an empty region?",
                "Which child owns the midpoint, and can either child equal the parent?",
                "What changes for odd versus even lengths?",
              ],
              reveal: "Half-open bounds make size equal stop - start and empty equal start == stop. Assign the midpoint consistently to exactly one child and verify every non-base child is smaller than its parent for odd and even sizes.",
            },
          ],
          glossary: [
            { term: "divide and conquer", definition: "A design that splits a problem into smaller regions, solves them, and combines their evidence." },
            { term: "invariant", definition: "A statement about the candidate region that remains true after every reduction." },
            { term: "half-open interval", definition: "A region [start, stop) including start and excluding stop." },
            { term: "monotone predicate", definition: "A truth pattern that changes direction at most once across an ordered domain." },
            { term: "logarithmic", definition: "Growth proportional to the number of repeated constant-factor reductions, commonly halving." },
            { term: "combine cost", definition: "Work required to construct a parent answer from solved subproblems." },
          ],
          debugChecklist: [
            "Write the interval convention and candidate invariant beside the loop or recursive function.",
            "Trace empty, one-element, two-element, odd, and even regions.",
            "Check that every update either excludes or retains the midpoint consistently.",
            "Prove each discarded region cannot contain a valid answer using the structural promise.",
            "Verify every non-base child or next interval is strictly smaller than its parent.",
            "Include slice-copy and combination costs when evaluating time and space complexity.",
          ],
          checkpoint: {
            question: "Why can binary search discard half of a sorted candidate region?",
            options: ["Midpoints are always the answer", "Sorted order turns the midpoint comparison into evidence about an entire side", "Python lists automatically remember searched indexes", "Every list contains duplicate halves"],
            answerIndex: 1,
            explanation: "The ordering promise lets one comparison rule out a whole side while preserving the invariant that every possible answer remains in the candidate region.",
          },
        },
        coachConversation: [
          { learner: "Why can binary search discard half the values?", coach: "Only because a sorted or monotone promise lets the midpoint comparison speak for an entire region. Without that promise, the same discard can remove the answer." },
          { learner: "I keep getting an off-by-one error. What should I write first?", coach: "Choose one interval convention—such as half-open [left, right)—and state which indexes are candidates. Every midpoint update must preserve that invariant and make the interval strictly smaller." },
          { learner: "How do I know whether divide and conquer is actually faster?", coach: "Count the number of subproblems, the depth of repeated splitting, and the work used to combine results. Include slicing and copying costs; short source code does not guarantee low complexity." },
        ],
        documentation: [
          { label: "bisect", description: "Standard-library binary search for insertion points in sorted sequences.", url: "https://docs.python.org/3/library/bisect.html" },
          { label: "Sorting HOWTO", description: "Official guidance on sorted data, keys, stability, and ordering techniques.", url: "https://docs.python.org/3/howto/sorting.html" },
          { label: "timeit", description: "Measuring small Python code snippets when comparing implementations empirically.", url: "https://docs.python.org/3/library/timeit.html" },
        ],
        runbook: [
          runStep("1. Confirm the promise", "State whether input is sorted, monotone, recursively splittable, or otherwise structured.", "A counterexample showing why the algorithm needs that promise.", "Region elimination is valid only because structure lets local evidence speak for unseen elements.", "Construct the smallest unsorted or non-monotone counterexample and identify the exact discard decision it would invalidate."),
          runStep("2. Define regions", "Choose inclusive or half-open bounds and write exactly which indexes they contain.", "Initialization, size formula, and invariant using one convention.", "Consistent interval mathematics prevents skipped candidates, repeated midpoints, and out-of-range access.", "List candidate indexes explicitly for a four-item example, then check that initialization and every child bound represent the intended subset."),
          runStep("3. Justify reduction", "Explain what a middle observation or subanswer proves about each region.", "A proof for every discarded or combined half.", "Speed comes from discarded work, and each discard carries a correctness obligation to preserve all possible answers.", "Finish the sentence because middle has this relation, no index in this side can.... If it cannot be completed, do not discard that side."),
          runStep("4. Prove shrinkage", "Show that every non-base path produces strictly smaller regions.", "Traces for empty, one-, two-, odd-, and even-sized regions.", "Strict shrinkage guarantees termination and exposes midpoint updates that repeat the same two-element region forever.", "Calculate stop - start before and after every branch for a two-element case; each continuing branch must reduce it."),
          runStep("5. Budget the tree", "Count subproblems, depth, combination work, copying, and extra storage.", "Time and space complexity tied to the actual implementation.", "Halving depth does not automatically mean logarithmic total work when both children are visited or each level copies and scans data.", "Draw a level-by-level tree, sum work across each level, and separately record maximum active depth and temporary allocations."),
        ],
      },
      py12: {
        title: "Problem Solving & Dynamic Programming",
        subtitle: "Model choices as reusable states, then evaluate them in a provably safe order.",
        objectives: [
          "Decompose an unfamiliar problem into state, choices, transitions, and base cases.",
          "Distinguish exhaustive search, justified greedy selection, memoization, and tabulation.",
          "Recognize one-dimensional, grid, capacity, subset, and two-sequence dynamic-programming patterns.",
        ],
        tutorial: [
          section(
            "Model before choosing an algorithm",
            "Begin with the contract, not a favourite technique. Identify what a complete answer must report, which information changes between smaller versions of the problem, and which facts can be forgotten without changing future choices. That smallest sufficient description is the state. Next list every legal choice, the state created by each choice, and the direct answers for boundary states. A brute-force decision tree is often the clearest first model because it exposes completeness before optimization.",
            "def route_states(stops):\n    states = []\n    for position in range(stops + 1):\n        choices = []\n        if position + 1 <= stops:\n            choices.append(position + 1)\n        if position + 3 <= stops:\n            choices.append(position + 3)\n        states.append((position, choices))\n    return states\n\nprint(route_states(4))",
            [
              "Write the return contract and one tiny hand-worked answer.",
              "Name a state using only information future choices require.",
              "List legal choices and base cases before optimizing.",
            ],
            "Mental model: an algorithm is a controlled walk through a graph of states, not a collection of clever lines.",
            "A state that omits necessary history merges different subproblems and can make a fast implementation confidently return the wrong answer.",
          ),
          section(
            "Use greedy choices only with a proof",
            "A greedy algorithm commits to one locally attractive choice and never revisits it. This is safe only when an exchange argument shows that some optimal solution can start with that choice without becoming worse. When the objective is to fund the greatest number of independent projects under one budget, replacing a funded expensive project with a cheaper available project cannot reduce the count or violate the budget. Coin systems do not generally have an equivalent promise, so taking the largest coin first can miss a smaller coin count.",
            "def fund_most_projects(costs, budget):\n    funded = []\n    spent = 0\n    for cost in sorted(costs):\n        if spent + cost > budget:\n            break\n        funded.append(cost)\n        spent += cost\n    return funded\n\nprint(fund_most_projects([8, 3, 6, 2, 9], 13))",
            [
              "State the local rule without relying on intuition words such as best.",
              "Search for a small counterexample before trusting the rule.",
              "Explain how an optimal solution can exchange its first differing choice.",
            ],
            "Mental model: greedy commits once; dynamic programming compares alternative futures before committing.",
            "A greedy strategy can pass friendly examples while failing the first input whose local improvement blocks a stronger later combination.",
          ),
          section(
            "Cache overlapping subproblems",
            "Optimal substructure means an optimal answer can be assembled from optimal answers to smaller states. Overlap means the same state is reached along several decision paths. Memoization starts with a recursive specification and stores answers on demand. Tabulation chooses an order in which every dependency is ready before its consumer. Both evaluate the same recurrence; their practical differences are reachability, call overhead, evaluation order, and storage layout.",
            "def count_hops(distance):\n    table = [0] * (distance + 1)\n    table[0] = 1\n    for position in range(1, distance + 1):\n        table[position] += table[position - 1]\n        if position >= 3:\n            table[position] += table[position - 3]\n    return table[distance]\n\nprint(count_hops(7))",
            [
              "Write each table entry's meaning as a complete sentence.",
              "Derive transitions from the final choice that reaches a state.",
              "Confirm dependencies are initialized before each table entry is calculated.",
            ],
            "Mental model: dynamic programming evaluates a directed acyclic dependency graph once per reachable state.",
            "Building a table without defining what each cell means produces indexes that look plausible but have no correctness argument.",
          ),
          section(
            "Recognize dimensions and safe compression",
            "One varying quantity produces a one-dimensional table; two prefixes, coordinates, or capacities often produce two dimensions. A sequence-comparison cell may depend on the cell above, left, and diagonally above-left. Space can be compressed when later rows need only a bounded number of earlier rows, but update direction becomes part of correctness. Descending capacity updates encode a one-use item; ascending updates allow reuse. Compress only after the full dependency picture is clear.",
            "def path_table(height, width):\n    ways = [[0] * width for _ in range(height)]\n    ways[0][0] = 1\n    for row in range(height):\n        for column in range(width):\n            if row > 0:\n                ways[row][column] += ways[row - 1][column]\n            if column > 0:\n                ways[row][column] += ways[row][column - 1]\n    return ways\n\nfor row in path_table(3, 4):\n    print(row)",
            [
              "Draw dependency arrows before replacing a full table with rolling storage.",
              "Explain how update direction controls whether current-stage values may be reused.",
              "Separate time complexity from auxiliary-space complexity.",
            ],
            "Mental model: storage layout is an implementation of dependency lifetime.",
            "Updating a compressed table in the wrong direction silently changes a zero-or-one choice into an unlimited-reuse choice.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "Solve a state graph once",
            body: "A problem becomes dynamic programming when many complete decision paths revisit the same smaller question. Define nodes as sufficient states and edges as legal choices. Base nodes have direct answers; other nodes combine answers from their outgoing dependencies. Memoization explores this graph on demand, while tabulation chooses a dependency-respecting order.",
            steps: [
              { label: "Contract", detail: "Specify the answer type, invalid-input policy, and smallest examples without mentioning an algorithm." },
              { label: "State", detail: "Name exactly the information that distinguishes one future subproblem from another." },
              { label: "Transition", detail: "Partition complete solutions by their first or final choice and map each group to smaller states." },
              { label: "Order", detail: "Choose recursion with a cache or a table order that evaluates every dependency before use." },
              { label: "Audit", detail: "Prove coverage and exclusivity, then count states, transitions per state, and retained storage." },
            ],
          },
          guidedPractice: [
            {
              title: "Design a cafeteria tray recurrence",
              prompt: "A diner moves through stations and may take the next station or skip exactly one station, collecting points without visiting adjacent stations. Define states and a recurrence without implementing a graded maximum-sum function.",
              starterCode: "points = [4, 9, 3, 8]\n# state(i) means the best result using ...\n# final choices for state(i): ...\n# base states: ...",
              questions: [
                "Which earlier boundaries are needed when the current station is taken?",
                "Why does the recurrence represent both taking and skipping every station?",
                "How many prior state values must remain live during left-to-right evaluation?",
              ],
              reveal: "Describe the state over a prefix boundary. The final decision either skips the last station and keeps the previous prefix answer, or takes it and combines its points with the answer before its neighbour.",
            },
            {
              title: "Choose direction for a capacity table",
              prompt: "A workshop has one copy of each component. Explain whether remaining capacities should be updated upward or downward and demonstrate the difference on one component.",
              starterCode: "capacity = 6\ncomponent_weight = 3\ncomponent_value = 8\nbest = [0] * (capacity + 1)\n# trace one upward pass and one downward pass",
              questions: [
                "Which direction can read a value written by the same component?",
                "What real-world rule does reading that fresh value encode?",
                "How would the direction change if unlimited copies were available?",
              ],
              reveal: "Descending updates read only states from before the current one-use component. Ascending updates can read a value just improved by that component, which intentionally models unlimited reuse.",
            },
          ],
          glossary: [
            { term: "state", definition: "The minimum information needed to identify one reusable subproblem completely." },
            { term: "transition", definition: "A rule connecting a state to smaller states through legal choices." },
            { term: "optimal substructure", definition: "A property where an optimal answer can be assembled from optimal answers to subproblems." },
            { term: "overlapping subproblems", definition: "Repeated decision paths that ask for the answer to the same state." },
            { term: "memoization", definition: "Top-down evaluation that caches a state answer the first time it is calculated." },
            { term: "tabulation", definition: "Bottom-up evaluation that fills states in a dependency-respecting order." },
            { term: "exchange argument", definition: "A proof that replaces a choice in an optimal solution with the greedy choice without worsening it." },
          ],
          debugChecklist: [
            "Write one sentence defining every state dimension and its valid range.",
            "Calculate base states directly and verify their table indexes.",
            "For each transition, name the final choice represented and confirm the dependency is smaller or already evaluated.",
            "Trace a counterexample to any proposed greedy rule before implementing it.",
            "Check whether update direction models one-use or reusable resources.",
            "Test empty inputs, zero capacity or target, impossible states, and one-element boundaries.",
            "Count distinct states and work per state rather than counting only source lines.",
          ],
          checkpoint: {
            question: "What is the strongest reason to use dynamic programming instead of raw recursive enumeration?",
            options: ["The function name becomes shorter", "Repeated decision paths ask the same well-defined subproblems", "Every loop is automatically greedy", "Tables remove the need for base cases"],
            answerIndex: 1,
            explanation: "Caching or tabulating a precisely defined repeated state avoids recomputing the same subtree while preserving the original recurrence.",
          },
        },
        coachConversation: [
          { learner: "How do I know what belongs in the state?", coach: "Ask whether two partial histories with the same proposed state always have exactly the same legal futures and answer. If not, the state forgot information; if yes, extra history can be discarded." },
          { learner: "Should I always use a table instead of recursion?", coach: "No. Write the recurrence first. Memoization is natural when only part of an irregular state space is reachable; tabulation is useful when a compact dependency order is clear and recursion depth is undesirable." },
          { learner: "Why does loop direction matter in knapsack-style tables?", coach: "A compressed table mixes earlier-stage and current-stage values. Descending updates prevent a one-use item from reading its own fresh result; ascending updates deliberately permit reuse." },
          { learner: "Can I trust a greedy rule if it passes many tests?", coach: "Tests provide evidence, not the missing proof. Try to construct a counterexample, then supply an exchange or staying-ahead argument tied to the problem's structural promise." },
        ],
        documentation: [
          { label: "functools.cache", description: "Official unbounded memoization decorator for hashable function arguments.", url: "https://docs.python.org/3/library/functools.html#functools.cache" },
          { label: "functools.lru_cache", description: "Official bounded or unbounded cache with reuse statistics.", url: "https://docs.python.org/3/library/functools.html#functools.lru_cache" },
          { label: "Data structures", description: "Official coverage of list, set, dictionary, and looping techniques used for state storage.", url: "https://docs.python.org/3/tutorial/datastructures.html" },
          { label: "timeit", description: "Official tool for measuring equivalent implementations after reasoning about complexity.", url: "https://docs.python.org/3/library/timeit.html" },
        ],
        runbook: [
          runStep("1. Restate the contract", "Define valid inputs, exact output meaning, and three tiny examples before naming a technique.", "A compact input-output table including an empty or zero boundary.", "A precise contract prevents an attractive algorithm from solving a neighbouring but different problem.", "Remove story vocabulary temporarily and describe the mathematical objects, constraints, and requested result."),
          runStep("2. Draw choices", "Enumerate legal decisions from one partial solution and sketch the resulting decision tree.", "A labelled tree whose leaves correspond to complete valid candidates.", "Exhaustive structure establishes coverage and reveals where different paths revisit equivalent futures.", "Shrink the input until every branch fits on paper, then label each edge with the decision it represents."),
          runStep("3. Define reusable states", "Merge tree nodes only when they have identical future choices and answer meaning.", "A state sentence, recurrence, and direct base values.", "Correct state equivalence is the foundation for both caching and safe table cells.", "Find two histories mapped to one state and ask whether any future choice can distinguish them; add the missing dimension if it can."),
          runStep("4. Select evaluation strategy", "Choose justified greedy commitment, memoized search, or dependency-ordered tabulation.", "A reason tied to exchange safety, reachability, dependency order, and recursion limits.", "The strategy should follow the state graph and proof obligations instead of fashion or surface syntax.", "Implement the tiny brute-force model first, compare outputs, and inspect repeated states before optimizing."),
          runStep("5. Verify and budget", "Test boundaries and counterexamples, then count states, transitions, and retained storage.", "A correctness paragraph plus O(state count × work per state) and space analysis.", "Dynamic programming improves repeated work only when the number and cost of distinct states are understood.", "Instrument a small run to record visited states and compare that set with the dimensions predicted on paper."),
        ],
      },

      py13: {
        title: "Pygame Game Lab",
        subtitle: "Turn tested Python rules into a responsive Snake-inspired game loop and a small 2D platformer.",
        objectives: [
          "Set up and run a small Pygame project locally while keeping browser-runnable game rules separate from the desktop display adapter.",
          "Trace event, intent, update, collision, draw, present, and timing responsibilities without mixing previous, candidate, and committed state.",
          "Use elapsed time, Rect geometry, explicit modes, and deterministic spawn rules to build repeatable Snake and platformer mechanics.",
          "Model temporary power-ups, pause and restart transitions, debugging evidence, and platformer motion as testable state changes.",
        ],
        tutorial: [
          section(
            "Prepare and prove a real Pygame workspace",
            "Pygame is a third-party package, so a normal Python installation does not guarantee that import pygame will work. Create a project directory, create and activate a virtual environment, then install with python -m pip install pygame. Using python -m pip ties the installer to the Python interpreter you selected. Keep a small main.py entry point, game rules in ordinary modules, and optional assets in a project-relative assets directory. Prove the interpreter with python -c \"import pygame; print(pygame.version.ver)\" before running python main.py. A graphical program begins with pygame.init(), creates a display Surface with pygame.display.set_mode(...), and always reaches pygame.quit(), ideally through a try/finally boundary. The EduGround browser runner executes pure rule examples but cannot open the desktop SDL window, so passing browser checks and completing one local window smoke test are two separate pieces of evidence.",
            "try:\n    import pygame\nexcept ModuleNotFoundError:\n    print(\"Local setup needed: python -m pip install pygame\")\nelse:\n    print(\"Pygame version:\", pygame.version.ver)",
            [
              "Create and activate a project-specific virtual environment.",
              "Install and prove Pygame with the same interpreter used to launch main.py.",
              "Keep pure rule modules browser-testable and perform one local window open, event, and clean-shutdown smoke test.",
            ],
            "Mental model: the desktop Pygame adapter owns devices and pixels; ordinary Python modules own gameplay decisions and remain runnable without a window.",
            "Running pip from one interpreter and main.py from another creates a misleading import failure even when installation reported success.",
          ),
          section(
            "Give every frame ordered jobs and one time unit",
            "A real-time program repeats a controlled pipeline. First consume pygame.event.get() so quit and one-time transitions are not lost. Then sample held controls, convert both sources to plain intent, advance state, resolve collisions, draw committed state, call pygame.display.flip() or update(), and measure the frame with clock.tick(max_fps). tick returns milliseconds, so divide by 1000 before multiplying a pixels-per-second velocity by elapsed seconds. Clamp unusually large elapsed values after a pause or debugger stop, or use a fixed simulation step with an accumulator when deterministic physics matters. Keep an explicit scene mode such as title, running, paused, or game_over: paused still consumes events and draws, but deliberately skips gameplay timers and physics.",
            "speed_px_per_second = 120\nframe_times_ms = [16, 33, 50]\nposition = 0.0\nfor elapsed_ms in frame_times_ms:\n    elapsed_seconds = min(elapsed_ms, 100) / 1000\n    position += speed_px_per_second * elapsed_seconds\n    print(f\"{position:.2f}\")",
            [
              "Consume the complete event queue on every frame.",
              "Convert milliseconds to seconds before using per-second velocity or timers.",
              "Let explicit modes decide which updates run, then present and tick exactly once per rendered frame.",
            ],
            "Mental model: rendering may vary in duration, but one frame still forms an event → intent → candidate → resolved → committed → presented transaction.",
            "A frame-rate cap only limits how quickly frames repeat; it does not convert pixels per frame into a stable real-world speed.",
          ),
          section(
            "Make Snake, spawning, and power-ups replayable",
            "A Snake is an ordered collection of grid coordinates with the head first. One update translates the head by a direction vector, then applies wrapping, food, and self-collision policies before committing a new body. Food and power-ups need the same invariant: never spawn on an occupied coordinate. Hide unpredictability behind one injected seed or use a stable candidate sequence, so a recorded seed plus input sequence reproduces the same failure. A temporary boost is ordinary state such as remaining_ms and multiplier; subtract elapsed time during running updates, clamp at zero, and emit an effect-expired transition once. Drawing a glowing pickup or playing a sound responds to a committed event but does not decide whether collection occurred.",
            "occupied = {(0, 0), (1, 0), (2, 0)}\ncandidates = [(1, 0), (3, 0), (4, 2)]\nspawn = next((cell for cell in candidates if cell not in occupied), None)\nremaining_ms = 600\nfor elapsed_ms in (200, 250, 300):\n    remaining_ms = max(0, remaining_ms - elapsed_ms)\n    print(spawn, remaining_ms)",
            [
              "Create candidate Snake state before deciding whether the frame may commit.",
              "Choose spawn locations from explicit free cells using a replayable seed or order.",
              "Store power-up duration and mode in gameplay state; keep particles, colours, and sounds as feedback effects.",
            ],
            "Mental model: the same previous state, input sequence, elapsed steps, and spawn seed must produce the same next states.",
            "Calling the global random generator from drawing code makes replays drift and can place pickups before occupied-state changes are resolved.",
          ),
          section(
            "Build and debug platformer motion with Rect evidence",
            "Platformer input should produce intent or velocity rather than teleporting the player. Pygame screen coordinates normally grow downward, so a local Rect-based jump begins with negative vertical speed and gravity adds a positive amount each update. The browser platformer studio deliberately displays height above ground as positive upward; its adapter converts that height to screen-y before drawing, so the inspector and local convention are never silently mixed. Keep floating-point world coordinates for smooth delta-time integration, then synchronize an integer pygame.Rect for collision and drawing. Rect.colliderect answers overlap, but edge contact alone is false and overlap alone does not say whether to resolve left, right, top, or bottom. Name the four player edges before resolving: feet are y, head is y plus height, and the side edges are centre x minus or plus half-width. Compare each previous edge with its candidate edge. Downward feet crossing a platform top means snap feet to top; rising head crossing an underside means snap head below it; a right or left edge crossing a solid side means snap that side outside. Then zero only the velocity component on the resolved axis. The four collision drills in the studio create one controlled crossing each, highlight the responsible body strip, and print the contacted platform so a beginner can connect geometry evidence to the repair. Debug real play by printing previous, candidate, and committed edges for only the failing object. Close each browser mission by recording one prediction, the first trace mismatch, and the rule that transfers unchanged into a thin local Pygame adapter. Treat game feel as redundant feedback about committed facts: persistent text or shape evidence must survive muted audio, unavailable colour, reduced motion, zoom, and keyboard-only play, while optional effects never change physics or timing.",
            "player_y = 44\nplayer_height = 24\nvertical_speed = 7\ngravity = 2\nplatform_top = 80\nnext_speed = min(vertical_speed + gravity, 12)\ncandidate_y = player_y + next_speed\ncrosses_top = player_y + player_height <= platform_top <= candidate_y + player_height\nif crosses_top:\n    candidate_y = platform_top - player_height\n    next_speed = 0\nprint(candidate_y, next_speed)",
            [
              "Keep float world position and the Rect collision representation synchronized in one named adapter step.",
              "Use movement direction and crossed boundaries to choose a collision response instead of treating every overlap as landing.",
              "Name the contacted body part and platform, then zero only the velocity component on that collision axis.",
              "Record previous, candidate, and committed edges plus the active scene mode before changing tuning constants.",
              "Debrief one browser prediction, transfer only the pure rule, and audit persistent feedback with sound and decorative motion disabled.",
            ],
            "Mental model: physics proposes a float position, Rect geometry supplies collision evidence, resolution commits one legal world snapshot, and the adapter presents that fact through more than one adjustable cue.",
            "Rounding world position repeatedly or adding loud, motion-heavy feedback before the state is legible both hide evidence; keep one float truth and make optional effects subordinate to persistent cues.",
          ),
        ],
        deepDive: {
          mentalModel: {
            title: "The two-layer game loop",
            body: "Keep the game as a deterministic state machine surrounded by a Pygame adapter. The adapter receives events and held keys, turns them into plain intent, asks rule functions for next state, then draws and plays effects from the returned result. This boundary makes gameplay runnable in a terminal test even when no window, controller, image, font, or speaker exists.",
            steps: [
              { label: "Receive", detail: "Consume events and sample current controls without changing gameplay state inside device-specific conditionals." },
              { label: "Interpret", detail: "Translate keys or buttons into plain direction, jump, pause, restart, and quit intent." },
              { label: "Advance", detail: "Calculate candidate position, velocity, Snake body, timers, score, and mode from the previous snapshot." },
              { label: "Resolve", detail: "Apply wrapping, collisions, landing, goal, hazard, and state-priority rules before committing the frame." },
              { label: "Present", detail: "Draw the committed snapshot, update the display, play queued feedback, and limit or measure frame time." },
            ],
          },
          guidedPractice: [
            {
              title: "Predict, trace, and debrief a Snake food frame",
              prompt: "A three-segment Snake moves right toward food. Predict one committed field first, then trace candidate head, growth decision, next body, score event, and collision question without writing a complete graded function. Record the first phase that confirms or rejects the prediction.",
              starterCode: "body = [(40, 20), (20, 20), (0, 20)]\ndirection = \"right\"\nfood = (60, 20)\ncell_size = 20\n# candidate_head = ...\n# ate_food = ...\n# next_body = ...",
              questions: [
                "Why must the food decision be known before the old tail is removed?",
                "Which body coordinates should the new head be compared with for self-collision?",
                "Which initial state, ordered input, elapsed step, and seed belong in the replay packet?",
                "What pure rule transfers unchanged, and what input or drawing responsibility belongs only to a local Pygame adapter?",
              ],
              reveal: "Translate the old head by one cell, compare that candidate with food, preserve the tail only for growth, then ask collision questions about the deliberately constructed next snapshot. A useful debrief names the first mismatching phase and transfers the rule without transferring browser presentation code.",
            },
            {
              title: "Investigate a fast platform crossing and its feedback",
              prompt: "A falling player moves far enough in one frame that the final rectangle would appear below a thin platform. Trace both bottom edges, decide whether a landing occurred, convert the world result for local Pygame drawing, and plan how the player perceives the landing without requiring audio, colour, or decorative motion.",
              starterCode: "player_y = 18\nplayer_height = 30\nvertical_speed = 16\nplatform_top = 58\n# previous_bottom = ...\n# candidate_y = ...\n# candidate_bottom = ...\n# crossed_top = ...",
              questions: [
                "Which inequality proves that the platform top lies along the movement path?",
                "Why must horizontal overlap be checked independently?",
                "What exact y and vertical speed should be committed after a valid landing?",
                "Which persistent text or shape cue still communicates landing when sound is muted and reduced motion is enabled?",
                "Which coordinate conversion belongs in the adapter instead of the physics rule?",
              ],
              reveal: "Use previous_bottom <= platform_top <= candidate_bottom while falling. If horizontal spans overlap too, commit the legal world position and zero vertical speed, then let the adapter translate coordinates and expose the same landing fact through persistent redundant feedback.",
            },
          ],
          glossary: [
            { term: "event queue", definition: "Ordered notifications about device and window transitions that the program consumes during each frame." },
            { term: "frame", definition: "One complete pass from input through committed game state to a presented image." },
            { term: "Surface", definition: "A Pygame pixel canvas that can receive drawing operations or be copied onto another surface." },
            { term: "Rect", definition: "An integer axis-aligned rectangle with synchronized edge attributes and collision helpers." },
            { term: "delta time", definition: "Measured elapsed time used to relate motion or animation to real time rather than render count." },
            { term: "sprite", definition: "A visual game object commonly paired with an image, rectangle, update behaviour, and optional groups." },
            { term: "tunnelling", definition: "Missing a collision because an object moves from one side of a thin obstacle to the other between checks." },
            { term: "state machine", definition: "A finite set of named modes plus explicit events that permit or reject transitions between those modes." },
            { term: "deterministic replay", definition: "Reproducing a run from the same initial state, input sequence, timing steps, and pseudo-random seed." },
            { term: "effect event", definition: "A committed gameplay fact such as boost-collected that presentation adapters may turn into sound, particles, or vibration." },
            { term: "replay packet", definition: "The smallest saved initial state, seed, ordered inputs, and timing steps needed to reproduce a gameplay trace." },
            { term: "redundant feedback", definition: "Communicating one important state change through multiple compatible cues instead of relying on colour, sound, or motion alone." },
            { term: "game feel", definition: "Responsive and legible presentation feedback attached to committed actions without becoming the source of gameplay truth." },
          ],
          debugChecklist: [
            "Confirm the active Python interpreter can import pygame before debugging window code.",
            "Consume events every frame and keep exactly one clear quit path.",
            "Record the scene mode, input intent, delta time, spawn seed, previous state, candidate state, and committed state separately.",
            "Check coordinate origin, positive axis directions, rectangle size, and whether each velocity uses pixels per frame or pixels per second.",
            "Trace held-key conflicts and prevent airborne jumps when the design allows only grounded jumping.",
            "Test edge contact, corner contact, containment, fast crossings, empty groups, and leaving the level.",
            "Draw temporary Rect outlines and log only the object that first violates an invariant instead of printing every object forever.",
            "Call display presentation and clock tick once per complete frame rather than once per object.",
            "Replay a failure with a recorded seed and finite input list before changing gravity, speed, or collision constants.",
            "Repeat the critical path with sound muted, reduced motion, high zoom, and keyboard-only control while preserving the same gameplay result.",
            "Check that success, damage, pause, expiry, and failure each provide persistent text or shape evidence in addition to optional animation or audio.",
          ],
          interactiveLab: {
            kind: "pygame-frame-tracer",
            title: "Trace one complete game-state transaction",
            description: "Choose a Snake, platformer, or power-up scenario. Move through receive, interpret, advance, resolve, and present while revealing only the evidence available at that phase.",
            defaultScenario: "snake-food",
            phases: [
              { id: "receive", label: "1 · Receive", prompt: "Which transition events, held controls, and elapsed duration are available?" },
              { id: "interpret", label: "2 · Interpret", prompt: "What plain movement, jump, pause, or collection intent follows?" },
              { id: "advance", label: "3 · Advance", prompt: "What candidate coordinates, velocity, body, timer, score, and mode are calculated?" },
              { id: "resolve", label: "4 · Resolve", prompt: "Which boundary, collision, spawn, expiry, or state-machine rule repairs the candidate?" },
              { id: "present", label: "5 · Present", prompt: "What committed state is safe to draw, announce, and convert into feedback effects?" },
            ],
            controls: [
              { id: "direction", label: "Direction intent", type: "select", values: ["up", "right", "down", "left"], defaultValue: "right" },
              { id: "elapsedMs", label: "Elapsed milliseconds", type: "range", min: 10, max: 250, step: 10, defaultValue: 50 },
              { id: "jump", label: "Jump transition", type: "toggle", defaultValue: false },
              { id: "wrap", label: "Snake wrapping", type: "toggle", defaultValue: true },
            ],
            scenarios: [
              {
                id: "snake-food",
                label: "Snake reaches food",
                previous: "head=(40,20), length=3, food=(60,20), score=20",
                intent: "direction=right; one grid update is due",
                candidate: "head=(60,20), food contact=True, old tail preserved",
                committed: "length=4, score=30, mode=running, food-respawn requested",
                teachingNote: "Food contact is decided before the tail rule, so growth, score, and the respawn event describe one transition.",
              },
              {
                id: "snake-spawn",
                label: "Seeded power-up spawn",
                previous: "occupied={1,5,9}, seed=4, current power-up cell=11",
                intent: "collection event requests the next free spawn",
                candidate: "seed advances to 5; ordered search starts at cell 4",
                committed: "power-up cell=4, boost_remaining_ms=1500, score=50",
                teachingNote: "The same occupied cells and seed reproduce cell 4, making a spawn defect replayable without relying on screenshots.",
              },
              {
                id: "platform-land",
                label: "Fast fall crosses a platform",
                previous: "screen bottom=48, downward vy=16, platform top=58",
                intent: "horizontal spans overlap; elapsed step is accepted",
                candidate: "next bottom passes platform top 58",
                committed: "bottom=58, vy=0, grounded=True, mode=running",
                teachingNote: "Previous and candidate edges prove a downward crossing before Rect state is snapped to the platform.",
              },
              {
                id: "power-expiry",
                label: "Temporary boost expires",
                previous: "mode=running, boost_remaining_ms=180, multiplier=2",
                intent: "advance running timers by elapsed_ms=250",
                candidate: "remaining_ms would become -70 without a boundary",
                committed: "boost_remaining_ms=0, multiplier=1, effect-expired event emitted once",
                teachingNote: "Clamping before the mode decision creates one precise positive-to-zero transition for feedback and replay.",
              },
              {
                id: "paused-window",
                label: "Paused window remains responsive",
                previous: "mode=paused, player=(84,120), score=70, boost_remaining_ms=900",
                intent: "quit and resume events are consumed; elapsed_ms=200 is observed",
                candidate: "presentation may update, but gameplay update is deliberately skipped",
                committed: "player=(84,120), score=70, boost_remaining_ms=900, mode=paused",
                teachingNote: "Pause freezes simulation state rather than the outer loop, so the window can announce its mode, receive resume, and still shut down cleanly.",
              },
              {
                id: "accessible-checkpoint",
                label: "Checkpoint feedback survives muted play",
                previous: "mode=running, checkpoint=1, sound_enabled=False, reduced_motion=True",
                intent: "player overlaps checkpoint 2; collection rule commits once",
                candidate: "checkpoint=2 and checkpoint-reached effect event are proposed",
                committed: "checkpoint=2, persistent text and shape cues enabled, optional animation suppressed",
                teachingNote: "The rule commits one fact while the adapter supplies redundant feedback; muting audio or reducing motion never changes progress.",
              },
            ],
            invariants: [
              "The previous snapshot remains unchanged until every rule for the frame has been evaluated.",
              "Rendering reads committed state and never acts as the authoritative physics, spawn, timer, or score update.",
              "Paused mode consumes events and presents feedback while deliberately preserving gameplay ticks and temporary-effect timers.",
              "The same initial state, input sequence, elapsed steps, and spawn seed produce the same committed trace.",
              "Removing optional sound, colour-only emphasis, or decorative motion does not change the committed gameplay result or block the next action.",
            ],
          },
          checkpoint: {
            question: "Which design makes a platform landing rule easiest to test without opening a Pygame window?",
            options: ["Read the keyboard inside the collision function", "Pass plain positions, velocity, sizes, and platforms to a rule function", "Draw the player twice and compare screenshots", "Play a sound whenever any rectangles share an edge"],
            answerIndex: 1,
            explanation: "Plain-data inputs and returned state isolate the deterministic rule from input devices, rendering, audio, and display availability.",
          },
        },
        coachConversation: [
          { learner: "Do I need Pygame inside every game function?", coach: "No. Keep movement, score, gravity, and state priority as plain-data rules. Use Pygame at the boundary for events, Rect adapters, drawing, timing, images, and sound." },
          { learner: "Why does movement change when the frame rate changes?", coach: "Pixels per frame describe render count rather than real time. Use a stable fixed update or multiply velocity by measured elapsed seconds, then keep a sensible frame-rate cap." },
          { learner: "Why does my player sometimes fall through a platform?", coach: "Checking only final overlap can miss a thin obstacle. Compare previous and candidate bottom edges while falling, require horizontal overlap, and resolve the first crossed platform top." },
          { learner: "Should jump use KEYDOWN or get_pressed?", coach: "Use a transition such as KEYDOWN or an explicit just-pressed action for one jump, then use held state for smooth horizontal movement. The grounded flag prevents unintended air jumps." },
          { learner: "How can a random power-up bug be repeatable?", coach: "Give the spawn rule its own seeded generator or ordered candidate source, record that seed with the input trace, and keep random calls out of drawing. The same state and seed should choose the same free cell." },
          { learner: "Why keep title, paused, running, and game-over as strings?", coach: "Named modes make legal transitions visible. They also let pause continue processing events and drawing while deliberately skipping physics, score, and temporary-effect timers." },
          { learner: "What should I write down after a browser lab?", coach: "Keep one prediction, the first trace phase that confirmed or rejected it, the invariant that explains the result, and the pure rule you will transfer. A screenshot alone cannot show update order or reproduce timing." },
          { learner: "Does adding more particles and sound always improve game feel?", coach: "No. First make the committed event immediate and readable with persistent text or shape evidence. Add optional motion and sound afterward, and verify that muted or reduced-motion play preserves the same information and controls." },
        ],
        documentation: [
          { label: "Pygame events", description: "Official event queue and Event object reference for responsive frame loops.", url: "https://www.pygame.org/docs/ref/event.html" },
          { label: "Pygame Rect", description: "Official rectangle geometry, movement, clipping, and collision reference.", url: "https://www.pygame.org/docs/ref/rect.html" },
          { label: "Pygame time", description: "Official Clock, frame limiting, elapsed-time, and timer-event reference.", url: "https://www.pygame.org/docs/ref/time.html" },
          { label: "Pygame display", description: "Official display initialization, presentation, mode, and window-management reference.", url: "https://www.pygame.org/docs/ref/display.html" },
        ],
        runbook: [
          runStep("1. Prove environment and contracts", "Activate one environment, prove its interpreter and Pygame version, smoke-test a clean window shutdown, then list every state field, unit, invariant, and legal scene transition.", "Interpreter and package evidence plus a state table for position, velocity, timers, seed, score, and allowed events per mode.", "Separating adapter faults from explicit state contracts prevents debugging the wrong layer and keeps effects from becoming hidden truth.", "Print sys.executable and pygame.version.ver, then replace drawing with title, running, paused, game-over, and restart transition logs."),
          runStep("2. Trace one timed frame", "Write event, held input, intent, delta conversion, update, collision, commit, draw, present, and clock phases in their exact order.", "A numbered loop skeleton with previous, candidate, and committed snapshots plus one stated time unit.", "Stable order and units prevent systems from reading half-old state or moving at render-dependent speeds.", "Use a finite list of frame times such as 16, 33, and 50 ms and print the accumulated position without opening a window."),
          runStep("3. Extract deterministic rules", "Move movement, collision, spawn selection, power-up timers, score, and mode priority into functions using plain values and an injected seed.", "Rule tests and a replay fixture that pass without importing Pygame or opening a display.", "Pure reproducible rules shrink the graphical debugging surface and make awkward boundaries repeatable.", "Record one seed and input list, run it twice, and compare every committed state rather than only the final score."),
          runStep("4. Integrate one vertical slice", "Connect one tested mechanic to events, Rect synchronization, drawing, and feedback while displaying collision outlines and state labels.", "One playable movement, spawn, boost, jump, or landing mechanic with an on-screen mode and timer readout.", "Small vertical slices reveal adapter mistakes without mixing several incomplete systems.", "Disable decorative assets, use solid rectangles, and log only the first invariant violation for this mechanic."),
          runStep("5. Debrief, transfer, and audit feel", "Test quit, pause, restart, awkward collisions, deterministic spawns, effect expiry, win, and loss; record each browser prediction and first mismatching trace phase, transfer the unchanged rule behind a local Pygame adapter, then repeat the critical path with sound muted, reduced motion, high zoom, and keyboard-only control.", "A replay packet, defect-to-fix trace, browser-to-local responsibility map, measured tuning log, and feedback matrix for success, warning, pause, expiry, failure, and recovery.", "Evidence-led transfer prevents a new adapter from hiding rule regressions, while redundant feedback keeps committed facts perceivable across settings without changing gameplay timing.", "Start with one checkpoint effect event, present it through persistent text and shape plus optional sound, then verify that disabling optional channels does not alter state, controls, or timing."),
        ],
      },
    },
  };
})();
